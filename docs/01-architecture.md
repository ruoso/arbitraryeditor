# Arbitrary Composer Editor — Architecture

> Companion to `00-design.md` (the UI/UX design, D1–D18). This doc covers *how
> the editor is built*: language, toolkit, threading, and the project layout.
> Status: **architecture decided (A1–A6); not yet scaffolded.**

## 1. The one idea

The editor **invents almost no new systems** — it plumbs `libarbc`'s existing
concurrency and data model into the dockspace. The library already owns the hard
parts: a shared, versioned `Document` (doc 14), transactional edits with undo, an
off-thread interactive renderer, a **single-writer** tile cache, a shared
`WorkerPool`, and a checkpointed mmap workspace (doc 15). The editor is a **host**
that wires those to windows and input — not a re-implementation of any of them.

## 2. Binding — native C++, linked directly

**A1.** The editor is a **native C++20 application that links `arbc::arbc`
directly** (`find_package(arbc CONFIG REQUIRED)`, as `examples/host-interactive/`
does). No FFI, no language seam: a canvas holds real `HostViewport` / `Document` /
`Backend` objects and shares memory with the renderer. For a compositor moving
tiles every frame, any cross-language boundary would sit exactly on the hot path.

## 3. Toolkit — Dear ImGui + SDL3 + OpenGL (GLES3/WebGL2 subset)

**A2.** The UI is **self-rendered** on **Dear ImGui (docking branch) + SDL3 +
OpenGL**, rendering to the **GLES3 / WebGL2 common subset**.

*Why:* the driving constraint is **native now, self-rendered WASM later, one
codebase** (whole UI drawn to a GPU canvas, identical native and in a WebGL
canvas). That rules out DOM/hybrid (wrong kind of web) and native-widget
toolkits, and favors a self-rendering GPU UI that compiles to both via Emscripten.
Among those, ImGui+SDL is the proven path: its **docking branch is the D18 model**
(split-tree, tabs, drag-dock) out of the box, multi-canvas tiles-as-textures is
trivial, SDL gives native + Emscripten platform layers from one source, and the
dependency is lean and vendorable. Slint (retained, also web-capable) and
Qt-for-WebAssembly were weighed; Slint adds unknowns (newer, Rust core, custom
canvas + docking to verify), Qt-WASM adds weight (binary size, SharedArrayBuffer
threading friction that bites a thread-heavy library).

*WASM is not scoped now* — the choice only keeps it **reachable by construction**.

**A3. Don't-block-WASM seams (cost nothing today).** Native-first, but structure
so the web port is swap-in implementations, never a rewrite:

- **Self-rendered UI** — no native widgets; the whole editor is ImGui draw data.
- **Platform behind SDL** — window, input, GL context, main loop; SDL has native
  and Emscripten backends.
- **`PlatformServices` interface** — file/directory access, threading spawn, and
  clock behind a thin interface. Native impl = std threads + filesystem; the
  later web impl = **File System Access API / OPFS** + Emscripten **pthreads**
  (Web Workers + `SharedArrayBuffer`, needing COOP/COEP on the host).
- **GLES3/WebGL2 render subset** — the same GL code runs native and on WebGL2.
- **Workspace through the library abstraction** — the editor never assumes real
  `mmap`; it goes through `libarbc`'s workspace/document API, so the browser
  workspace backing (OPFS/in-memory arena) is a *library* port (doc 15), not an
  editor concern.

## 4. Threading & data flow — obey the library's contract

**A4.** The editor adopts `libarbc`'s concurrency rules verbatim (docs 02/14/15):
the tile cache is **single-writer / render-thread-confined**, worker dispatch is
**leaf-only**, there is **one shared `WorkerPool`**, and **one `HousekeepingThread`
per `Document`** checkpoints the workspace. Threads:

```
  UI thread            input · dockspace layout · paint frames · SUBMIT edits
      │  (never touches the cache directly)
      ▼
  Transaction ─▶ Document (writer)  ──────────────▶ damage
      │  undo/redo = doc-14 journal, free                 │
      ▼                                                   ▼
  per-canvas driver   HostViewport + InteractiveRenderer  (own cache, replans)
      │  leaf renders                                     │
      ▼                                                   ▼
  shared WorkerPool ─▶ tiles ─▶ (that canvas's) cache ─▶ frame ─▶ GL texture ─▶ screen
      ▲
  HousekeepingThread  checkpoints the mmap workspace
```

Edits flow UI → writer → damage → renderers; frames flow renderers → UI. The UI
thread stays responsive because rendering is never on it.

**A4.1 — single writer *identity*, not serialized access** (libarbc doc 15 § Thread
rules; ruoso/arbitrarycomposer#7). "Single writer" means every structural write to a
`Document` must originate from **one stable OS-thread identity** for the document's
lifetime — not merely from access an external mutex serializes. libarbc's lock-free
allocator/checkpoint path is written against one mutator (relaxed `high_water`,
non-atomic `SlabDirectory::publish`, the writer-thread checkpoint seal); a consumer
mutex only re-covers the accesses it wraps, and a checkpoint firing on a bounced
writer identity can seal the wrong chunk frontier. Debug builds bind the identity on
first write and assert it. **Consequence for the editor:** a design that writes from
two threads (e.g. UI-thread `transact` + a render-thread `settle_external_loads`
publish) must **funnel both to one writer thread via task-posting**, never take turns
under a lock. Reads stay lock-free via `pin()` and the copy-on-write content-binding
snapshot (arbc#10/#11); only *writes* are identity-bound.

**A4.1a — the identity rule is not a licence to drop the data-race guard.** A4.1
retires turn-taking as a *fix for writer identity*; it does **not** say a UI edit and
a driven render iteration may run concurrently. libarbc v0.2.0's COW covers exactly
one thing — the `d_contents` binding read — and `editor.canvas.single_writer` over-read
it into "no lock at all", which left two non-COW pieces of shared mutable state that a
commit and a `HostViewport::step()` both touch, both TSan-confirmed races on the
shipped `CanvasHost`:

- **`HostViewport::DamageAccumulator`** — a `Transaction::commit` on the UI thread
  `push_back`s into the same `std::vector<Damage>` that the render thread's `step()`
  swaps out in `drain()`. arbc calls it "single-owner, drained on the driving thread",
  i.e. it assumes writer == driver. This is a plain data race, **not** an identity
  question, and no library-side identity fix retires it.
- **`Model::set_commit_sink`** — `step()`'s `settle_external_loads` opens a
  `JournalSuspension` that rewrites the very commit-sink pointer a concurrent
  `Transaction::commit` reads (arbc#13). Live, not latent: any document with deferred
  external children settles on the render thread while the UI edits.

So `CanvasHost` held a **writer-priority document lease** — exclusive for the length
of an `apply_edit` mutation and for the length of a `run()` iteration
(`canvas_host.cpp`). It was a *mutual-exclusion* guard only; it made neither race
observable and made no claim about writer identity, which stays exactly as A4.1
states (arbc#7/#13, upstream-owned). The one thing it must not repeat from the v0.1.0
`doc_mu` is starvation: a plain mutex let `run()`'s re-armed loop barge ahead of a
queued edit, freezing a streamed burst, so the render side additionally yielded to any
waiting writer. **Retired by `editor.canvas.writer_thread`** (see A4.1b): the lease,
`CanvasHost::apply_edit` and the edit seam itself are gone from `render`.

**A4.1a (amended at the v0.3.0 pin) — both races are now library-fixed; the lease
guards nothing and is retained only until the writer thread lands.** libarbc v0.3.0
(`editor.canvas.arbc_v030`) closes both items above at their real owner, the library:
`DamageAccumulator::flush()`/`drain()` each take a mutex held across a bounded append
or a swap only (`arbc/runtime/host_viewport.hpp:319-337`, arbc#13's companion), and
`HostViewport::step()` **no longer publishes off the writer thread** — it asks
`Model::on_writer_thread()` and, when the answer is no, settles nothing and reports the
owed install as `StepOutcome::external_loads_ready` (`host_viewport.cpp:164`, arbc#13).
So the lease's *justification* is retired: it is a guard over two races that no longer
exist. Its **code** is retired separately, by `editor.canvas.writer_thread`, which
removes the concurrency rather than guarding it — deleting the guard first would leave
the editor with two writer identities and no mutual exclusion, strictly worse than the
state this row describes.

The bump also changes **where an external arrival installs**, and this is now the
governing statement: a `Document`-bound `HostViewport` installs a **writer-thread
settler** at construction (`Document::set_external_load_settler`, install-counted,
released in the dtor), and the document runs it at the next `Document::begin()` —
i.e. an arrival installs on the writer thread ahead of the next edit, never on the
render thread. Ignoring `StepOutcome::external_loads_ready` therefore costs latency,
never correctness. In the editor this path is **armed but unreachable today**: nested
cells are in-document `ObjectId` children (never a serialized `params.ref`) and the
only load path passes a `FilesystemAssetSource`, which resolves inline, so
`Document::external_loads_ready()` is always zero and the settler always early-outs.
`writer_thread` is what makes it both reachable and correct. Writer *identity* is
unchanged from A4.1 — and note that `Model::on_writer_thread()` answers **true while
unbound** ("the caller would become the writer"; binding happens in `Model::Transaction`'s
ctor and `Model::navigate()`, nowhere else), so "the UI thread transacts before the
render thread ever steps" is a load-bearing ordering rule, not an incidental one.
Realized by `editor.canvas.arbc_v030`; superseded in part by `editor.canvas.writer_thread`.

**A4.1a (amended at the v0.4.0 settler split) — the settler no longer installs *at
construction*; the render thread constructs the `HostViewport` and posts only an explicit
`attach_settler`/`detach_settler` pair.** libarbc v0.4.0 (`editor.canvas.arbc_v040`, arbc#25)
adds `HostViewport::Config::install_settler` and `HostViewport::attach_settler()`/
`detach_settler()` — precisely the split D-writer_thread-8 asked upstream for. Consumed by
`editor.canvas.settler_attach_split`: the viewport is constructed on the **render thread**
with `install_settler = false`, so its ctor/dtor do **no** writer-thread-only work and the
render thread owns the viewport lifetime (RAII, no `submit_sync` round trip for
construction). The two writer-thread-only concerns the ctor/dtor used to fold in — the
document's external-load settler slot (`set_external_load_settler`, install-counted) **and**
the registration of the viewport's `DamageAccumulator` into the shared `DamageRouter` whose
registrant list a commit's flush walks — move into the explicit `attach_settler()` (posted
`submit_sync` on add / resize-in) and `detach_settler()` (posted on remove / resize-out /
teardown). **Delivery is two-phase, because arbc#25/v0.4.0 shipped only the settler-slot
half.** `install_settler = false` defers the settler slot alone; `DamageRouter::register_sink`
/ `set_damage_sink` stay welded to the ctor with no internal synchronization, so
render-thread construction still races the router's registrant list — which is why the
settler consumer (`editor.canvas.settler_attach_split`) re-deferred on the falsified
assumption that one flag deferred both. The router-registrant half moves off the ctor only
when a successor libarbc release lands it behind an `install_settler`-style flag (or a
dedicated `attach_router`/`detach_router` pair): pinned by
`editor.canvas.arbc_router_split_pin` and consumed by
`editor.canvas.arbc_router_attach_split`, which together complete this amendment. What does
**not** change: the install is still writer-thread-only, still
install-counted, still never on the render thread, and the N-viewports-per-document
single-slot / last-installer-wins rule (A4.1b) is untouched — only *which call* performs
the install moves off the ctor onto `attach_settler()`. The prior amendment's "installs at
construction … released in the dtor" wording is superseded accordingly. Realized (settler
half) by `editor.canvas.settler_attach_split`; the router-registrant half is completed by
`editor.canvas.arbc_router_split_pin` (the successor pin) + `editor.canvas.arbc_router_attach_split`
(the consumer).

**A4.1b — the document owns ONE writer thread; the UI and render threads are pure
submitters and readers.** A4.1's prescribed consequence is now the shipped shape.
`ace::writer::WriterThread` (L1, over `base` + `platform` only — it holds no `Document`
and names no libarbc type) owns one OS thread and a FIFO closure queue with two entries:
`submit_sync` blocks until the closure has run, `submit` enqueues and returns. Both push
onto the *same* queue, so submission order is total and cross-entry. Sync is the default —
most call sites are result-carrying and capture by reference, where async would be a
lifetime bug rather than a latency win; async is reserved for a gesture already coalescing
into one undo step and for the render thread's arrival nudge, which must never block a
frame. It is spawned through `platform::Threads` (the seam A3 reserves for the Emscripten
port), and constructed *without* one it runs every submission inline on the caller — the
degenerate mode that keeps the headless Catch2/golden fixtures thread-free AND is the WASM
fallback, where a browser main thread may not block on `Atomics.wait`: still one identity,
still correct, no blocking wait.

The posting inventory is **everything libarbc documents WRITER-THREAD ONLY**, which is
wider than "writes": every `transact`/commit, `journal().undo()/redo()`, `Document::
checkpoint()`, `capture_snapshot` (a *read*, but it copies the writer-owned content
side-map and unknown-field stash), `set_damage_sink` / `set_external_load_settler`, and —
first and most load-bearing — `arbc::load_document`, because the first write BINDS the
identity. The writer therefore starts **before** the `AppState` is built and stops
**after** the last canvas is gone and before the document is released; stopping drains the
queue rather than discarding it. Reads stay lock-free and unposted: `pin()`, `resolve()`,
`for_each_content()`, `serialize_snapshot` over an already-captured snapshot,
`external_loads_ready()`, `on_writer_thread()`, and (post-arbc#15) `can_undo()`/
`can_redo()`. So save posts its cheap half and serializes off-thread.

Two consequences reach further than the seam. The `KindBridge` an external arrival interns
through is **document-scoped and writer-owned**, not per-renderer: the document holds ONE
settler slot, so a per-canvas bridge would both split ownership across threads and intern
into whichever viewport installed last. And the writer consumes the deferred settle
**proactively** — it polls `Document::external_loads_ready()` (any-thread, one relaxed
load) whenever its queue drains, staying armed only while a fetch is outstanding, so an
arrival on a completely idle app is consumed with no submission from anywhere. The render
thread's per-frame report and arbc's own auto-settler at the next `begin()` are two more
idempotent paths to the same install; none is load-bearing alone, which is the point.
Realized by `editor.canvas.writer_thread`.

## 5. Multi-canvas — N observers, one document

**A5.** A canvas view is **one `HostViewport` + `InteractiveRenderer` over the
shared `Document`**; multiple canvases are **multiple renderers sharing one
`WorkerPool`** (`runtime.shared_worker_pool` is exactly this). Each cache stays
render-thread-confined, so multi-canvas needs **no new locking** — "paint through
Viewport ‖ look through Hero" (D18) is two renderers, two cameras, one document.
Selection and the shared panels are **project-level**, not per-canvas (D19): a
canvas is *only* a camera and carries no selection or inspection state.

The app is **single-project per process** (D19): opening another project **execs
a new instance**, so there is no multi-document state to manage — the app owns
exactly one `Document` for its lifetime, and the GC root-set is trivially that
document. (WASM analog: a project is a tab/instance.)

## 6. Display path & backend

**A6.** `CpuBackend` yields CPU tile surfaces; the canvas view **uploads them as
GL textures** (GLES3/WebGL2) and composites to the pane. A **GPU `Backend`** later
(rendering straight into textures) is behind the `Backend` seam — **no editor
change** (the display path already speaks textures). This is the doc-09 promise
the editor gets for free.

*(Amended by `editor.canvas.arbc_v040` at the v0.4.0 pin: the `Backend` seam grew
two virtuals — `composite_windowed` and `clear_rect` — that a pixel-modelling
implementation must supply, and `render_offline` now renders through the **tiled
driver** the interactive loop and the sequence exporter already ran, so there is
now **one render path** with `Exactness` the only axis separating offline from
interactive. Both changes are **inert for the editor**: it implements no
`arbc::Backend` and constructs only `arbc::CpuBackend`, and its only
`render_offline` calls are the 2-arg `(document, viewport, backend)` overload —
so A6's **no editor change** promise holds, and the sole `src/`-visible effect is
four justified golden re-baselines (`look_through_shot_64x64`,
`camera_manip_recrop_64x64`, `contact_sheet_3cam`, `contact_sheet_latin1` — all
minification / re-crop / thumbnail composites, where the retired untiled path
sized its temp to exactly the content and the tiled path disagree at a fractional
composite phase; the 1:1 and integral-phase composites stay byte-identical). The
structural claim above — `CpuBackend` tiles → GL textures → pane — is unchanged.
One clause is now weaker and recorded for the future: a **GPU `Backend`** later
would owe the two new virtuals, so "no editor change" holds for the display path
and the seam's shape but not for the labour of writing a future pixel-modelling
backend. There is no such backend today and no leaf that builds one — a human
design call if and when a GPU backend is scheduled.)*

Project I/O and plugins are thin wiring: open a project **directory** → the
library maps `workspace/`; Save → serialize to `project.arbc` + `assets/`; "Clean
up" → `gc_project_directory` (D16); plugin kinds load via the `Registry` seam and
populate "insert cell."

## 7. Project layout & components

Directory-per-component under `src/`, one dir per levelized component (§8), so
the level DAG is enforceable by path:

```
arbitraryeditor/
├── CMakeLists.txt      FetchContent libarbc (git ref) + Dear ImGui (docking) + SDL3
├── scripts/gate        the universal per-task gate (levels · format · build · ctest)
├── docs/               00-design.md · 01-architecture.md · mockup.html
├── src/
│   ├── base/           value types, ids, small helpers            (L0)
│   ├── platform/       PlatformServices seam + native impl        (L0)
│   ├── gl/             GLES3/WebGL2 abstraction                   (L0)
│   ├── project/        libarbc Document, project-dir open/save/gc (L1)
│   ├── scene/          cells · cameras · z-order                 (L1)
│   ├── interact/       hit-test · gizmo · snapping · brush math   (L1, UI-agnostic)
│   ├── commands/       actions → transactions · undo · selection  (L1)
│   ├── dockmodel/      view registry + layout + tool selection    (L1)
│   ├── render/         HostViewport/InteractiveRenderer · tile→GL (L2)
│   ├── views/          ImGui panels (canvas/layers/inspector/…)   (L3)
│   ├── dock/           dockspace shell + tool rail (ImGui docking)(L3)
│   └── app/ main.cpp   bootstrap · main loop · wiring             (L4)
├── tests/              Catch2 (L1 unit) · goldens · ImGui Test Engine e2e
└── assets/             icons, fonts (bundled)
```

Build: native desktop (Linux/macOS/Windows) first; `libarbc` arrives via
FetchContent from a git ref (a local checkout during co-development, a released
tag once its editor-facing API stabilizes). An Emscripten preset is added when
WASM is scoped, reusing everything but `platform/` and the host page (COOP/COEP).

## 8. Components & levelization

The editor is **levelized** — its own component DAG (not the library's kernel
L0–L6) — and levelization is the mechanism that **enforces the testability seam
(A8)**: the UI-agnostic core lives in components structurally forbidden from
including ImGui / GL / SDL, so "testable headless" is a compile-time invariant,
not a hope.

```
 L4  app · main            bootstrap · main loop · wiring (SDL+GL+ImGui)
 L3  dock · views          ImGui draw code — the ONLY layer that sees ImGui
 L2  render                HostViewport/InteractiveRenderer glue · frame-sync · tile→GL
 L1  project · scene ·     ── the UI-agnostic CORE (no ImGui/GL/SDL) ──
     interact · commands ·    unit-tested headless
     dockmodel · writer
 L0  base · platform · gl  value types · PlatformServices seam · GL abstraction
```

| Component | L | May depend on | ImGui/GL |
|---|---|---|---|
| `base` | 0 | — | no |
| `platform` | 0 | base | no |
| `gl` | 0 | base | GL only |
| `writer` | 1 | base, platform (**no libarbc**) | no |
| `project` | 1 | base, platform, **libarbc** | no |
| `scene` | 1 | base, project, libarbc | no |
| `interact` | 1 | base, scene, libarbc | **no** |
| `commands` | 1 | base, project, scene | no |
| `dockmodel` | 1 | base, platform | **no** |
| `render` | 2 | base, project, scene, gl, writer, libarbc | GL, not ImGui |
| `views` | 3 | scene, interact, commands, render, dockmodel, **imgui** | yes |
| `dock` | 3 | dockmodel, views, imgui | yes |
| `app` / `main` | 4 | everything | yes |

**All of L1 is the testable core** and none of it may `#include <imgui.h>` (or
GL/SDL). Enforced by a `check_levels`-style lint over the directory-per-component
layout, adapted from the library's `scripts/check_levels.py`.

## 9. Testing & definition of done

Verification is **layered**, not one gate:

| Layer | What | How |
|---|---|---|
| L1 logic (the bulk) | app-state, project, camera/cell/brush math, selection, dock model | **Catch2** unit tests — headless, no ImGui/GL |
| Rendered output | export, canvas composition | **golden** compare, reusing `libarbc`'s byte-exact `render_offline` |
| End-to-end UI | open→dock→select→drag→paint→look-through→export | **Dear ImGui Test Engine** (`ocornut/imgui_test_engine`), headless, drives widgets by ID + screenshot capture |
| Threading & smoke | UI↔driver handoff, per-platform "init + N frames" | **ASan/TSan** + headless smoke (offscreen GL: SDL dummy / EGL-surfaceless / llvmpipe) |

**Definition of done — encoded in every task, not a separate one.** The harness
and the levelization check are **not standalone leaves**; they are **acceptance
criteria on every leaf**. `foundation.build` stands up the Catch2 harness, the
`check_levels` lint, the component skeleton, and a single `scripts/gate`;
`app_shell` (the first ImGui surface) adds the **ImGui Test Engine** e2e harness
+ the offscreen-GL smoke. So from the first task onward every leaf's definition
of done is:

- **respects the levelization DAG** (`check_levels` clean);
- **lands its tests** — L1 logic → Catch2 unit; renders → golden; has UI → an
  ImGui Test Engine e2e; threads → sanitizer-clean;
- **clang-format + build clean.**

The orchestrator runs `scripts/gate` after every task; each refinement's
Acceptance-criteria section names the *specific* tests that instantiate this DoD
for that leaf. *(The ImGui Test Engine license is resolved — free under the OSI
open-source carve-out; see decision A10.)*

### 9.1 The offscreen software-GL ASan lane

The `clang-asan` CI lane runs the headless smoke/e2e tests (SDL3 `offscreen`
driver + Mesa `llvmpipe` software GL) under AddressSanitizer/UBSan. Three
environment requirements make it work; each masked the next when it was missing,
so they are recorded here rather than rediscovered:

1. **clang 20, not the distro clang 18.** Ubuntu noble's default clang 18 ships
   **no static ASan/UBSan runtime**, so the `asan` preset fails to *link*
   (`libclang_rt.asan*.a` not found). The clang lanes pin **clang 20** with
   `libclang-rt-20-dev` (from apt.llvm.org). This is set in *both*
   `.github/act/runner.Dockerfile` (the local `act` image) and the ci.yml
   "install clang-20" step, so GitHub and the orchestrator use the same toolchain.

2. **`llvm-symbolizer` on `PATH` in the runner image.** Without it, sanitizer
   reports are `<unknown module>` frames — undebuggable, and LSan suppressions
   (which match on function names) cannot match. The image installs `llvm-20`
   and exposes the unversioned `llvm-symbolizer` name the runtime auto-discovers.

3. **A sane `RLIMIT_NOFILE` on the `act` container.** Docker inherits the host's
   systemd `nofile` (~1e9, "infinity"). The sanitizer forks `llvm-symbolizer`,
   whose child `close()`s every fd up to that limit before `exec` — so a
   symbolizing ASan test spins through ~1e9 `close()` syscalls and looks hung.
   `orchestrator/driver.py` passes `--ulimit nofile=65536:65536` to `act`.
   GitHub-hosted runners already have a sane limit, so this is act-only.

**Mesa driver leaks are suppressed, not fixed.** LSan reports residual leaks that
are **not** editor bugs: `Shell::shutdown()` does the full teardown (ImGui
backends → ImGui context → GL context → window → `SDL_Quit`) and the tests invoke
it. The bytes are owned by the Mesa driver, which no application call can free —
`eglInitialize` caches its `_EGLDisplay` past `eglTerminate`, and `llvmpipe`
keeps first-draw JIT/rasterizer state for the process lifetime. `tests/lsan.supp`
suppresses these, scoped to the two named entry frames
(`SDL_EGL_InitializeOffscreen`, `ImGui_ImplOpenGL3_RenderDrawData`) so LSan stays
fully active for the editor's own allocations; it is wired in via `LSAN_OPTIONS`
in the ci.yml test step.

## Decisions log

| # | Decision |
|---|---|
| A1 | Native C++20 app links `arbc::arbc` directly — no FFI. |
| A2 | Self-rendered UI on **Dear ImGui (docking) + SDL3 + OpenGL (GLES3/WebGL2 subset)**; native-first, WASM reachable by construction. |
| A3 | Don't-block-WASM seams: self-rendered UI, platform-behind-SDL, a `PlatformServices` interface (file/threads/clock), GLES3/WebGL2 subset, workspace via the library abstraction. WASM not scoped now. |
| A4 | Adopt the library's concurrency verbatim — single-writer cache, leaf-only dispatch, shared pool, per-doc housekeeping. UI thread submits edits, never touches the cache. |
| A5 | Multi-canvas = N `HostViewport`/`InteractiveRenderer` over one `Document` sharing one `WorkerPool`; no new locking. |
| A6 | Display = `CpuBackend` tiles → GL textures; a GPU `Backend` later needs no editor change (behind the `Backend` seam). |
| A7 | **Process-per-project**: one process = one project (opening a project is a new `exec`); the app owns exactly one `Document` for its lifetime — no multi-doc management, GC root-set is that document. Selection + shared panels are **project-level**; canvases are only cameras (D19). |
| A8 | **Levelized components enforce the testability seam**: the editor has its own component DAG (§8); the UI-agnostic L1 core (project/scene/interact/commands/dockmodel) is structurally forbidden from including ImGui/GL/SDL, enforced by a `check_levels` lint. Only L3 (views/dock) sees ImGui. |
| A9 | **Layered testing as per-task acceptance criteria**: Catch2 L1 units + `render_offline` goldens + **ImGui Test Engine** headless e2e + ASan/TSan. The harness + `check_levels` + `scripts/gate` are stood up by `foundation.build` and are the **definition of done on every leaf**, not standalone tasks. |
| A10 | **ImGui Test Engine license — resolved: free under the OSI open-source carve-out.** `ocornut/imgui_test_engine` is dual-licensed (the `imgui_test_engine/` folder under the *Dear ImGui Test Engine License*; everything else MIT). That license is **permissive, not copyleft** — it imposes no terms on our own source and does not "infect" the editor; redistribution only requires carrying its copyright + license text. Its free tier explicitly covers software *released publicly under an OSI-approved open-source license*, **and** any entity with <$2M USD annual turnover; a paid license (from DISCO HELLO, after a 45-day trial) is required only by a **>$2M-turnover entity**. So as long as the editor ships under an OSI open-source license, linking the engine into the shipped binary (`IMGUI_ENABLE_TEST_ENGINE`, current build) is within the free terms — no test-only-link split needed. **Revisit only** if the editor is later distributed **closed-source by a >$2M-turnover entity**, in which case: buy a license or move the engine to a test-only link (the hooks are inert without an engine context). Supersedes the parking-lot entry (was D-app_shell-5). |
| A11 | **`dockmodel` owns headless UI state incl. active-tool.** `dockmodel` (L1, ImGui/GL/SDL/libarbc-free) is the headless UI-state model — view catalog + layout **+ the tool rail's active-tool selection** (a `ToolId` enum, a `tool_catalog()`, and a trivial `ToolSelection` holder). Placing the active-tool state here (not in `interact`) lets **`dock` own the *whole* rail** — modal tools **and** the view launcher — within the existing `dock → {dockmodel, views}` edges, with **no `interact` edge and no new component/DAG edge**. `interact` stays pure math (hit-test/gizmo/snapping/brush). The **tool→interaction dispatch** (active tool actually driving canvas behavior) is **promoted into `interact` when a canvas consumer exists** (`editor.canvas.tool_dispatch`, D20). Rail chrome is L3 `dock` per §7. *(Amended by `editor.canvas.tool_dispatch`: the levelization DAG forbids the literal reading — `interact:{base,scene}` (§8, `check_levels.py`) may not `#include` or name `dockmodel::ToolId`, so no `switch(ToolId)` can live inside `interact`. The promotion therefore **splits by layer, not by phrase**: the per-tool **behavior math** already lives in L1 `interact` (Pan → `pan`/`zoom`, Select → `pick`/`click_selection`/gizmo verbs, Brush → future `interact` stroke math), and the **branch that reads the active `ToolId` and routes among them** is a compile-time-exhaustive `switch` consumed at the L4 canvas consumer `app::CanvasView::draw_content` — the only site that sees both `dockmodel` and `interact`. The dispatch is a plain enum switch over the **closed** D20 modal set, **not** a polymorphic `Tool` vtable. No `interact→dockmodel` edge, no new component/DAG edge, `dockmodel::ToolSelection` stays a plain value. Realized by `editor.canvas.tool_dispatch`.)* |
| A12 | **Project-entry actions are dependency-inverted behind a `dock`-declared `ProjectGateway`.** The tool rail's New/Open/Recent buttons (D22) call an abstract `dock::ProjectGateway` (open / new / pick-folder / recent) that L3 `dock` **declares and owns**; the concrete impl lives in **L4 `app`** — the only level permitted SDL (`check_levels` `EXTERNAL_ALLOWED["sdl"] = {app}`) — and is the sole holder of the SDL-backed native folder dialog (`SDL_ShowOpenFolderDialog`, async-callback), the `platform::ProcessLauncher` + `current_executable_path()` (A7 / exec_new), the `dockmodel::RecentProjects` prefs store, and the L1 `project` validate/compose helpers. So the rail reaches process-launch + SDL through **one abstraction it declares**, not by reaching down into `commands` / `platform`; `dock`'s includes stay within its own header + std, and the L1 core never sees any of it. **This refines `editor.project.exec_new`'s Constraint 6**, which anticipated exposing `process_launcher()` (and a dialog faculty) on the shared `platform::PlatformServices` aggregate: the gateway is the better swap point — it keeps the interactive SDL dialog out of the L0 services aggregate (whose native impl must stay L0-constructible) and hands the WASM port (File System Access API) a single seam. **No new component and no new DAG edge** — `ProjectGateway` is a `dock` type; its impl is L4 `app`, which already closes over everything. |
| A13 | **`ProjectGateway` also carries the in-session Save + dirty query (extends A12).** The rail's **Save** button and its **dirty indicator** (the front-door verbs of D16 / §9 — "New / Open / Save / Save As; … a dirty indicator") join the *same* `dock`-declared `ProjectGateway` abstraction as the entry actions (`save() -> bool`, `is_dirty() const -> bool`), rather than being drawn as separate app chrome or reaching into `commands` from L3. Unlike New/Open/Recent — which spawn an L4 sibling process (A7/D19) — Save acts on the **in-process session** the L4 shell already owns: the L4 `AppProjectGateway` (which holds the one `AppState`, A7) implements `save()` by calling **L1 `commands::save_project(app_state, fs)`** and `is_dirty()` by reading `app_state.is_dirty()`. **Dirty = session revision-drift** (the workspace-vs-snapshot signal of D16): `AppState` holds a `saved_revision_` baseline — set at a `rebuilt_from_canonical` open (the workspace was just built from `project.arbc`, so it is clean) and updated on each successful publish; `is_dirty()` compares it against the live `document().pin()->revision()`. A fresh `create_project` or a workspace-mapped open has **no known-published snapshot this session** → dirty until the first Save (conservative: never a false-clean, so the user is never told unpublished edits are safely in `project.arbc` when they are not). The canonical dump itself is **L1 `project::save_project`** (re-emit `project.arbc` via `arbc::save_document` / `capture_snapshot`, atomic-publish through `FileSystem::atomic_replace`, owned tiles/assets to `assets/` through a `project`-side `arbc::AssetSink`). **No new component, no new DAG edge, no new external dependency** — two methods on an existing `dock` type, implemented in L4 `app`, backed by an existing L1 `project → arbc` edge. **Save As** (publish + copy the directory + exec a sibling on the copy, A7/D19) is its own leaf (`editor.project.save_as`), not this seam. *(Amended by `editor.project.reconstructing_reopen`: a mapped reopen is no longer *unconditionally* dirty — a cross-session published-revision sidecar (D28) lets an in-sync mapped reopen read **clean**, seeded into `saved_revision_` at `AppState` construction exactly as a `rebuilt_from_canonical` open is. Necessary because the reconstructing reopen (A19 amendment) makes the mapped open the **common** case, so leaving it unconditionally dirty would arm *modified* on essentially every reopen of a cleanly-saved project. The conservative asymmetry — never a false-clean — is preserved by write-ordering (canonical published, then sidecar). See D28.)* |
| A14 | **Cameras persist as an editor-defined libarbc `Content` kind in the `Document` — the editor's first custom kind.** *(Amended at the v0.3.0 pin, `editor.canvas.arbc_v030`: the codec-seam branch of the "**or**" below is the one that was taken — `editor.cameras.reopen_codec` registers the camera codec on the `Registry` directly, so the unknown-field-passthrough fallback is a recorded alternative, not the shipped path.)* D2 and §"How this maps onto `libarbc`" promise cameras "persist in the document (`project.arbc`) as scene objects" (`docs/00-design.md:519`), but libarbc's record set is fixed — `Composition`/`Layer`/`Content`/`LayerOrderChunk`, **no camera record** (`arbc/model/records.hpp:28-33`) — and `arbc::Viewport` is a **transient** compositor value (`arbc/compositor/compositor.hpp:16-36`), never a document object. So a **shot camera** is stored as **one `Content` of a new editor kind `org.arbc.camera` attached by one `Layer`** to the root composition: the **frame placement** is the `Layer`'s `Affine transform` (the D7 cell-shape — an affine you drag), the **output resolution** (W×H) + **name** are the `Content`'s serialized state, and the kind is **non-rendering** (contributes zero pixels — an observer, not paint; D2 click-through interior). A camera is therefore an `ObjectId`-addressable placed object *identical in shape to a cell* (D7 "one shape, one select tool"), so `editor.cells.selection` / `editor.cameras.manip` reuse the cell object/transform machinery (`document.hpp` `set_layer_transform`) rather than a bespoke camera path. Mutations (create · rename · "new shot from view") are **`commands` transactions** (D15 — a saved shot's framing IS scene data, undoable via the journal); the **transient viewport camera** (`app::CanvasView::Presenter::camera`, D-nav-1) is untouched session state. Persistence rides the **existing snapshot seam** (`project::save_project` → `arbc::save_document`/`capture_snapshot` over the `Registry` codec table, A13): the camera codec registers on the same `Registry`/codec seam as the builtin cell kinds — **or**, if the pinned `arbc`'s kind-registration surface does not admit an editor-authored codec, the camera persists through the **unknown-field passthrough** (`ContentSnapshot::unknown` → `PlaceholderContent`, non-rendering and transform-preserving for free), same observable roundtrip, no libarbc fork. The camera `Content` type + codec + read accessor (`scene::cameras(const Document&)`) live in **L1 `scene`** (its assigned home, §7); kind registration is wired at a level that already sees `scene` (`commands`/`app`), so `project`'s generic snapshot save serializes cameras with **no new `project→scene` edge, no new component, no new DAG edge, no new external dependency**. *Alternative rejected:* one `CamerasContent` list object holding all cameras — cleaner z-order separation, but a camera would then not be an `ObjectId`-addressable placed object, breaking the D7 uniform select/transform shape that `cells.selection` (selecting "a camera BORDER/label" through the *same* selection model) depends on. Realized by `editor.cameras.model`. |
| A15 | **An editor-defined *editable* kind forces rebuild-from-canonical on reopen — libarbc v0.1.0 exposed no per-kind state-slab walk hook (refines A14 / deviates from D-open-3 for custom kinds).** *(Amended at the v0.3.0 pin, `editor.canvas.arbc_v030`: **this row's premise no longer holds at the pinned version, and its "Future fix" precondition is met.** `arbc::KindStateWalker` has been on the `Registry` since **v0.2.0** — `arbc/contract/registry.hpp:118,146,164,182`, absent at v0.1.0 — and `Registry::add` now takes an optional `state_walker` (as A16 already records); `model.cpp` **collects** a non-inert `StateHandle` rather than asserting on it. The v0.1.0 reasoning below is retained as the historical record of why the editor-side policy exists. Whether the exposed hook actually serves the camera kind's state slab — and therefore whether the workspace-map fast path can be restored — is `editor.cameras.workspace_reopen_slab`'s call to make and verify; **until that leaf lands, the rebuild-from-canonical policy below remains in force**, because it is what ships today.)* A14's camera `Content` is an `arbc::Editable` that captures **live per-Content state** into a non-inert `arbc::StateHandle` (`scene/camera.hpp:101`, written via `txn.set_content_state` on rename). Persistence has **two** reopen routes (A13): the **canonical rebuild** (`load_document` over the `Registry` codec table — cameras restored to live typed `Content` by `editor.cameras.reopen_codec`), and the **workspace-map fast path** (`arbc::Document::open`, D-open-3's durable-by-default default when the workspace file is present). The fast path runs **no codec**; it re-binds the checkpointed `Model`, and `Model::rebuild_counts` **asserts every recovered `ContentRecord` carries an *inert* `StateHandle`** (arbc `model/model.cpp:771` — *"a persisted non-inert StateHandle needs a per-kind state-slab walk hook"*). **arbc `v0.1.0`** — the pin in force when this row was written — reserved that hook's location (`model.cpp:768-770`) but **did not expose it** (`Registry::add` admitted only factory/metadata/codec/binder), so a checkpointed workspace holding a camera **aborted (debug) / silently corrupted the handle (release)** on the map path — the built-in cell kinds dodge it only because their durable state rides the codec path, never the map. **Resolution (editor-side, fail-safe):** `project::open_project` **skips the map fast path and rebuilds from canonical whenever the caller registers editor kinds (a non-empty extra-kinds callback) *and* a canonical baseline exists** — the callback is the only crash-safe signal (an *unpublished* camera can sit in the map with canonical still camera-free, so canonical-content detection would fail open and abort). This trades the fast path's speed and its crash-recovery of *unpublished* edits (A13's dirty-recovery) for a correct, live reopen; for a camera-bearing workspace that is strictly better than the abort, for a camera-free editor session a conservative durability cost (pinned by a test). **Fallback:** when no canonical exists (a created-but-never-saved project, `project_open.cpp:199`) the map path is kept, leaving one residual the pinned lib cannot serve — a **never-saved project that holds a camera** (no canonical floor, no safe map), which the dirty model (A13/D16, "dirty until first Save") already frames as not-yet-durable. **Future fix (precondition now met — see the amendment above):** a libarbc release exposing the per-kind state-slab walk hook restores the fast path for custom editable kinds (cross-repo: a new `arbitrarycomposer` tag + an editor pin bump) — then A15's policy is superseded and the fast path carries a custom kind directly. The tag shipped in v0.2.0 and the pin bump in `editor.canvas.arbc_v030`; consuming it is `editor.cameras.workspace_reopen_slab`. **No new component, no new DAG edge, no new external dependency, no libarbc fork** — one guarded branch in L1 `project`. Realized by `editor.cameras.workspace_reopen_slab`. *(Amended by `editor.cameras.reopen_slab_adopt`: the v0.1.0 premise above is **retired** — a checkpointed non-inert `StateHandle` now reopens through `arbc::Document::open` with no abort, asserted at the pin — and the “Future fix” clause is answered **in the negative**: `KindStateWalker` does not restore the fast path, because the map path binds no `Content` for **any** kind. The guard stays in force for a larger, kind-agnostic reason now carried by **A19**, which is the operative rule; the text above is retained as the historical record of why the guard was first written.)* |
| A16 | **Cell insert is Registry-driven with no kind allowlist: L1 `scene` publishes a per-kind field schema (raw-config fallback for unknown kinds) and the L3 rail reaches it through two new `dock::ProjectGateway` virtuals (extends A12/A13).** `docs/00-design.md:505-511` closes Extensibility by naming the enforcement point — "the editor consumes kinds only through the `Registry` seam (`registry.ids()` + factory/codec/binder), **never a hard-coded kind set (see `editor.cells.model`)**" — but `arbc::ContentConfig` is a deliberately **opaque, kind-defined `std::string_view`** (`contract/registry.hpp:43`) and `Registry` advertises `factory`/`metadata`/`codec`/`binder`/`state_walker` and **nothing describing a kind's input fields** (`:142-164`), so "what do I ask the user for?" has no library answer. Resolution: **L1 `scene`** owns `insert_schemas(const Registry&, std::optional<project::CompositionSize>)`, which emits **one `KindInsertSchema` per `registry.ids()` entry, unconditionally and in registration order** — a kind the editor has a grammar adapter for (raster `"<w>x<h>"`, solid `"r,g,b,a"`, nested `"<decimal ObjectId>"`; `builtin_kinds.cpp:169-207`) gets named fields, and **every other kind gets a first-class raw-config fallback field** passed to its factory verbatim, so an unrecognized plugin kind is always insertable. Adapters are an **enhancement, never a gate**: there is no filter by id, by metadata, or by "is it visual", and a kind whose factory always fails (`org.arbc.fade`/`crossfade`) is still offered — its own error string is the UI (errors are values), with the `Document` untouched. Content is minted **only** via `registry.factory(id)`, never by naming a concrete arbc type; the accepted consequence is that a factory-built `org.arbc.solid` is **unbounded** (its config grammar admits no bounds) and therefore takes identity placement. `scene::add_cell(document, registry, kind_id, config, placement)` then follows the A14 mould exactly — factory → kind token via `project::seed_kind_bridge` → `Document::add_content` (self-committing, so a create is **two** journal entries, as for cameras) → one `transact` for `add_layer` + `attach_layer`; placement arrives as a finished `arbc::Affine` computed by the pure helper `interact::place_in_view` over primitives only (no `interact→scene` edge), which is the seam `editor.panels.overview` later swaps a drag-derived affine into and `editor.import.image` a native-px→units 1:1 affine, with **no change to `scene`**. Kind identity is read back through `KindBridge::lookup` on the `ContentRecord`'s token (`records.hpp:60-63` — `arbc::Content` has no kind-id virtual), `org.arbc.camera` layers filtered out of `scene::cells()`. **UI reach:** `dock` may not include `ace/commands` or `ace/scene`, so `ProjectGateway` gains `insert_kinds()` and `insert_cell(...)` exchanging **dock-local POD** (`InsertKindSpec`/`InsertFieldSpec`) exactly as `clean_up` exchanges `GcSummary` (`dock.hpp:32`, `:105`); the L4 impl marshals the L1 schema and runs the `commands::dispatch` inside `CanvasView::apply_edit` (edit_render_sync Constraint 1 / A4.1 writer identity). Keeping the schema in **L1** is what makes the no-allowlist property provable in headless Catch2 rather than only in an e2e — and structurally denies the L3 modal any place to keep an allowlist. This leaf registers **no new kind**, so A15's rebuild-from-canonical policy is untouched. *Alternative rejected:* a raw config text box for every kind — universal but makes "specify the resolution at insert" (`docs/00-design.md:116-119`) a hex-editor experience. *Alternative rejected:* an Insert **panel** as a ninth `dockmodel::ViewType` — the catalog is `editor.dock.view_registry`'s territory and inserting is a one-shot confirmed op (the two existing modals are the precedent). **No new component, no new DAG edge, no new external dependency, no libarbc fork.** *Future fix (cross-repo):* a per-kind insert-schema hook on `arbc::Registry` would shrink the adapter table to zero. Realized by `editor.cells.model`. *(Amended by `editor.cells.one_action_one_entry`: v0.4.0's `Document::create_content_and_attach` (arbc#20) collapses the two-entry create — the "`add_content` (self-committing, so a create is **two** journal entries) → one `transact` for `add_layer` + `attach_layer`" sequence above — into ONE journal entry and one undo press, with **no** intermediate published state in which a content exists attached to nothing; `scene::add_cell` and `scene::add_camera` both route through it (the no-allowlist / factory-first / placement-as-finished-`Affine` shape otherwise unchanged). The symmetric `Document::remove_contents(span<Removal>)` batch verb makes an N-object multi-select delete ONE entry and one undo too, so `commands::delete_selection` dispatches one `remove_cells_command` rather than a loop of N — overturning D-cells_remove-2's accepted N-entry asymmetry and restoring remove Constraint 3. D15 is already aligned and needs no edit.)* *(Amended by `editor.cells.insert_schema`: the editor's grammar-adapter table this row describes — raster `"<w>x<h>"`, solid `"r,g,b,a"`, nested `"<decimal ObjectId>"` (`builtin_kinds.cpp:169-207`) — is **retired**. v0.4.0's `Registry::insert_schema(id)` / `arbc::KindInsertSchema` (arbc#21) advertise each kind's fields (name / `Type{Integer,Number,Text}` / default / min-max / unit) plus an `assemble` thunk that turns the collected strings into the kind's `ContentConfig`, so the grammar stays the **kind's own**: `scene::insert_schemas` reads the library schema per id and `scene::build_config` delegates to `assemble`, and the editor no longer knows solid's separator is a comma. Validation moves off `build_config` onto the kind's factory — `assemble` only joins, so a malformed value is caught (as it always was) by `registry.factory(id)` at `add_cell`, errors-as-values. The enumeration is **unchanged**: `insert_schemas` still emits **one entry per `registry.ids()`, unconditionally and in registration order**; a kind advertising no schema (nested, fade, crossfade upstream) keeps the raw-config fallback (`k_raw_config_field`), so the no-allowlist property and its Catch2 witness stand. org.arbc.solid's grammar gains the extent it always supported (`"r,g,b,a,x,y,w,h"`, arbc#22, defaulting to a placeable `256×256`), which **retires D-cells_model-3's accepted unbounded-solid consequence** — a factory-built solid is now bounded and takes real `interact::place_in_view` placement rather than identity; the 4-field grammar still yields the unbounded "background fill". The editor registers an insert schema for its **own** `org.arbc.camera` kind (`scene/camera.cpp`) by the same rule it applies to every kind (a zero-field schema, its factory taking no config). The composition-matched resolution prefill (`size_initial`/`k_fallback_resolution`) is retired with the adapter table — the raster kind now advertises its own defaults, and `docs/00-design.md:116-119`'s "specify the resolution at insert" stays satisfied by the still-editable `width`/`height` fields. **No new component, no new DAG edge, no new external dependency** beyond the already-pinned v0.4.0, no libarbc fork. This closes the row's own "*Future fix (cross-repo): a per-kind insert-schema hook on `arbc::Registry` would shrink the adapter table to zero.*")* |
| A17 | **Hit-testing lives in L1 `interact`, split into a primitive-only pick *policy* and one `interact → scene` *assembly* adapter — the first and deliberately only use of that declared edge (refines A11, narrows A16's "no `interact→scene` edge").** The project-level selection (`commands::Selection`, D19/A5/A7) is keyed by `arbc::ObjectId`, so turning a pointer into a selection needs the document's **z-ordered stack of placed objects with their extents** — cells (`scene::cells`) *and* cameras (`scene::cameras`), because D7 makes them one shape under one select tool (A14). That stack cannot be assembled anywhere below L3 except in `interact`: `commands` may not depend on `interact` (`check_levels.py:32`), `scene` may not depend on `interact` (`:30`), and `base` may not include `arbc/` at all (`EXTERNAL_ALLOWED`), so no lower component can even *name* a target type carrying an `arbc::Affine`. Assembling it at L3/L4 instead would push the one piece of picking logic that must know about z-order, the cells/cameras split, and unbounded content out of Catch2 reach and duplicate it across the canvas, the Layers list, and the Overview. Resolution: **`interact` gains `ace/interact/pick.hpp`** with (a) a **primitive-only policy core** — `PickTarget{id, layer, kind, placement, extent}` plus `pick`/`pick_stack`/`pick_behind`/`marquee`/`click_selection`/`marquee_selection` over `std::span<const PickTarget>`, naming no `scene` type, so the Overview can feed it schematic boxes and a test can feed it hand-built targets — and (b) **one assembly adapter** `pick_targets(const arbc::Document&, const arbc::Registry&)` that includes `<ace/scene/cell.hpp>` + `<ace/scene/camera.hpp>` and is the **single** place the two lists are merged (cells in layer order, cameras appended above them — a camera renders zero pixels per A14, so its outline is always-on-top chrome). The `interact → scene` edge is already in the §8 DAG (`docs/01-architecture.md:210`, `check_levels.py:31`) and has simply never been exercised; A16's parenthetical "(no `interact→scene` edge)" is hereby scoped to what it was about — the pure geometry helpers (`place_in_view`, `hit_frame`, `recrop_frame`, …), which **stay primitive-only** so `editor.panels.overview` and `editor.import.image` can keep swapping their own affines in. The selection **state** stays in `commands` (D-app_state-3) and the mapping from a pick to a state change is returned as a value (`SelectionChange`) rather than applied in `interact`, so no `interact → commands` edge is created; L4 `app::CanvasView` applies it. **No new component, no new DAG edge, no new external dependency** — one new header in an existing L1 component, using an edge the DAG already declares. *Alternative rejected:* assemble the target list in L3 `views` / L4 `app` (keeps `interact` scene-free) — legal, but it puts z-order, the cells/cameras merge, and the unbounded-content rule in ImGui-linked code that headless Catch2 cannot reach, and duplicates it once per consuming surface. *Alternative rejected:* add a `commands → interact` edge and put picking beside `Selection` — a genuinely new DAG edge, and it drags interaction geometry into the transaction component. Realized by `editor.cells.selection`. |
| A18 | **The UI thread reads writer-owned `Document` structure only through a published, immutable snapshot built on the writer thread — never through libarbc's writer-thread-only inspection APIs (fills the gap A4.1's read clause leaves open).** A4.1 says *"reads stay lock-free via `pin()` and the copy-on-write content-binding snapshot (arbc#10/#11); only *writes* are identity-bound"* — true, but it reaches only the structures libarbc actually publishes. The journal's **entry vector is not one of them**: v0.3.0 published `can_undo()/can_redo()/depth()/cursor()` as relaxed atomics (arbc#15, `arbc/model/journal.hpp:106-114`) and deliberately left `entry_at()` writer-thread-only — *"this hands out a reference INTO the writer-owned entry vector, which a concurrent commit may reallocate. An off-thread history browser would need the entry list published copy-on-write; nothing needs that today (issue #15, explicitly out of scope)"* (`:120-125`, `d_entries` at `:159`). So the History panel's three per-frame `entry_at` calls were safe for one reason only — the UI thread *was* the writer — and `editor.canvas.writer_thread` removes exactly that. **Resolution (host-side, per libarbc doc 15's "publish the few words a frame samples; never publish a structure it would have to walk"):** the one `commands::AppState` (A7) owns a `HistoryPublisher` — an `std::atomic<std::shared_ptr<const HistorySnapshot>>` held through a `unique_ptr` so `AppState` stays movable — whose `refresh()` is **writer-thread-only** and whose `load()` is **any-thread**. The snapshot is a self-contained value (`names` + `cursor`, `depth == names.size()`) so a frame's list, highlight and dim-split always come from one generation; it is `const` and shared by pointer, so a reader holding it across any number of later commits sees a stable value — that immutability, not a lock, is what retires the reallocation hazard. Rebuild is skipped when the published `(depth(), cursor())` stamp is unmoved, which makes the refresh idempotent and therefore safe to call at every writer-turn exit. **Refresh point = the writer-turn epilogue, not the verb**: the L1 `commands` verbs (`dispatch`/`undo`/`redo`/`navigate_to`) refresh so the component is self-consistent headless, *and* `CanvasHost::apply_edit` gains an opaque `std::function<void()>` post-edit hook that L4 binds to `commands::publish_history` — because camera-inspector and manipulator edits call `scene::` transaction helpers inside a raw `apply_edit` closure and never pass through `commands` at all; `editor.canvas.writer_thread` moves that hook to its per-closure epilogue when `apply_edit` retires. Consequently L3 `views` reads **no** journal internals, and click-to-jump navigation moves into L1 as `commands::navigate_to(state, target_cursor)` (one clamped, end-stopped walk over the shipped single-step verbs — the library exposes no multi-step nav) so the whole jump is one writer-thread unit of work instead of N. **No new component, no new DAG edge** (`render` takes an opaque callback and never sees `commands`), **no new external dependency, no libarbc fork.** *Alternative rejected:* a consumer mutex around the journal — A4.1's own words, *"a consumer mutex only re-covers the accesses it wraps"*, and it would force every `scene`/`project` commit site under the same lock. *Alternative rejected:* a synchronous round-trip to the writer per frame to fetch the names — couples frame rate to edit latency, inverting A4's *"the UI thread stays responsive because rendering is never on it"*. *Future fix (cross-repo):* a libarbc release publishing an entry-name view copy-on-write would let the host drop the publisher; parked as a library judgment call, on nothing's critical path. Realized by `editor.canvas.history_published_reads`; extended by `editor.canvas.writer_thread`. *(Amended by `editor.canvas.history_snapshot_adopt`: v0.4.0's `Journal::history()` (arbc#24, `arbc/model/journal.hpp:79-90,161-174`) publishes the history PROJECTION library-side — an any-thread `std::shared_ptr<const HistoryView>` of `HistoryRow{name, byte_cost}` shared by pointer, swapped whole on every mutation — so the host `HistoryPublisher`/`HistorySnapshot`, the `publish_history` writer-turn refresh, and the L4 `CanvasView` post-edit hook are all **RETIRED**; the panel reads `journal().history()` through a thin any-thread L1 `commands::AppState::history()` accessor (`views` still includes no `arbc/model/journal.*` and never calls `entry_at()` — the rule stands, only its publisher moves from host to library, so the `entry_at`-copies-into-a-reallocating-vector hazard is now the library's to avoid, not the host's). Unlike the host snapshot, the library projection carries **no cursor**; the cursor is the separately-published any-thread `journal().cursor()`, so the accessor **clamps** it into `[0, rows.size()]` against the very snapshot it returns and derives the applied/redoable split and the undo/redo labels from that clamped value against the snapshot's own size — every index provably in-bounds despite two independent atomic loads, superseding the host-side cursor re-bundling above (whose premise — no library-published entry view — is now false; `depth()` is non-monotonic, so the clamp, not the library's publish ordering, is what bounds the index). `byte_cost` per row is newly available to the panel; DISPLAYING it is `editor.panels.history`'s call. **No new component, no new DAG edge, no new external dependency, no libarbc fork** — a net deletion plus one L1 accessor. Realized by `editor.canvas.history_snapshot_adopt`.)* |
| A19 | **The libarbc workspace-map reopen restores the RECORD GRAPH ONLY — it binds no `Content` for any kind — so a content-bearing project is reopenable only through the canonical rebuild (generalizes A15 from *editable editor kinds* to *all content*).** A15 forced rebuild-from-canonical because libarbc v0.1.0's map path **aborted** on a recovered non-inert `StateHandle`; at the v0.3.0 pin that abort is gone (`model.cpp:768-783` **collects** the handle) and the arbc#5 hook trio is present — `arbc::KindStateWalker` (`arbc/contract/registry.hpp:105-120`), `Model::recovered_content_state()` (`arbc/model/model.hpp:309-331`), `replay_recovered_content_state()` (`arbc/runtime/recovered_state_replay.hpp:44-53`). **The hook does not restore the fast path, and the reason is one level deeper than the state slab:** `arbc::Document::open(path, housekeeping)` takes **no `Registry`** and runs **no factory** (`arbc/runtime/document.hpp:76-85`), so the id→`Content` side-map starts empty and `resolve()` returns null for every recovered record while `for_each_content()` visits none. Verified at the pin for a **non-editable built-in** (`org.arbc.solid`, inert handle) and an **editable** kind (`org.arbc.raster`, non-inert handle) alike: the composition, the layer, the layer→content id, the layer transform and the persisted `StateHandle` slot all survive; the `Content` objects do not. At the editor's read seams the loss shows up in two shapes, both unusable: `scene::cameras()` is **empty** (it needs a `dynamic_cast` on `resolve`, so an unbound record simply disappears), while `scene::cells()` still **lists** each layer off the record graph with its kind token intact — D-cells_model-8's deliberate "an unknown-passthrough cell is still a cell" fallback — but with a null `resolve()` behind it, hence no extent, nothing to render, edit, or hit-test. The limitation is **kind-agnostic**: not about custom kinds, not about editable kinds, not about state slabs. **Consequences.** (1) The editor **registers no `KindStateWalker`**: the walker's `store` is contractually *“the kind's own **document-level** state store”*, while `scene::CameraContent`'s slab is a **per-instance** version table of heap `std::string`s (`scene/camera.hpp:97-111`) whose bytes are persisted by the A14 **codec** into `project.arbc`, never into the workspace arena — and `replay_recovered_content_state`'s input is unreachable anyway, since `arbc::Document` publishes no accessor for its `Model` (`document.hpp:398-410`; the attorney-client `HostViewportDocumentAccess` lives in the library-private `src/runtime/document_access.hpp`). A camera is a **codec-persisted** kind, not a **workspace-slab-persisted** one. (2) `project::open_project` keeps the map fast path **only** when the mapped workspace holds no content record the binding table could not serve; a content-bearing map is discarded in favour of `rebuild_from_canonical` whenever a canonical exists — for **every** caller, not just one that registers editor kinds, since the limitation is kind-agnostic. The extra-kinds callback survives only as a cheap short-circuit that avoids mapping a workspace the guard would reject anyway. (3) With **no** canonical (a created-but-never-saved project, which `create_project` leaves without `project.arbc`) there is nothing to rebuild from, so the mapped document is kept and its content is unrecoverable; `OpenedProject` therefore reports the count of content records the map could not bind — a **value, not an error** — so the loss is announced rather than silent. **No new component, no new DAG edge, no new external dependency, no libarbc fork, no pin bump** — one branch and one result field in L1 `project`. *Future fix (cross-repo):* a libarbc release making the map path registry-aware (`Document::open(path, registry, bridge)` reconstructing each `ContentRecord` through `registry.factory(kind_id)`) or exposing a public rebind seam would restore the fast path for all kinds at once; the library today states there is deliberately no rebind API (`arbc/pool/slot_store.hpp:119`). Until then D-open-3's “durable-by-default fast path” cannot deliver a usable document for a project with content. Realized by `editor.cameras.reopen_slab_adopt`. *(Amended by `editor.project.reconstructing_reopen` at the v0.4.0 pin: the "Future fix (cross-repo)" clause above is **answered — and its premise retired**. arbc#19 shipped exactly the registry-aware open it names: `project::open_project`'s map branch now opens through `arbc::open_document(path, registry, codecs, ctx, bridge)`, which reconstructs each recovered `ContentRecord` through the construction identity the record carries — captured at `add_content` via the kind's registered codec (`Document::set_content_identity_capture` / `arbc::codec_identity_capture`) — so `resolve()` returns live content and `for_each_content()` visits it. Consequences (2) and (3) are **superseded**: the content-bearing-map guard is **retired** (every caller maps-and-reconstructs, for **every** kind at once, restoring D-open-3's original durable-by-default policy — the extra-kinds callback no longer short-circuits the map, only augments the reconstruction registry), and `unbindable_content_records` **re-keys** from "records the map could not bind" to `arbc::ReopenedDocument::unreconstructed.size()` — a record with no captured identity (a pre-v0.4.0 workspace), no codec this session (a plugin absent), or a failed codec is left **UNBOUND and reported**, never a default stand-in. Consequence (1) stands: the editor still registers no `KindStateWalker`; the camera remains a codec-persisted kind. A second, owned effect — restoring the common-case map reopen flips it from clean to dirty — is resolved by the D28 cross-session published-revision sidecar, not an accepted false-dirty (A13 amendment). The record-only `arbc::Document::open` is unchanged and still pinned in `tests/arbc_pin_test.cpp`; reconstruction is a **different** entry point. The text above is retained as the historical record of why the guard was first written.)* |
| A20 | **Export is an L1 `commands` kernel driven by an *injected* renderer, writing PNG through the editor's ONE vendored encode dependency, on a `platform::Threads` job that publishes progress as an A18 snapshot.** D14 (`docs/00-design.md:481`, §9 prose `:355-367`) promises "pick camera(s) → render each at its resolution (`render_offline`) → file(s) … v1 PNG (sRGB8) … Heavy renders run async with progress", and §9's own testing table already assigns export a `render_offline` golden (`:300`). Four things had no home. **(1) Where the pipeline lives.** `commands` (L1) owns `ExportOptions`/`ExportItem`/`ExportPlan`, `plan_export`, filename sanitization, `encode_png`, `run_export` and the report; the impure render step arrives as `std::function<base::Srgb8Image(const arbc::Affine&, int w, int h, std::optional<Rgba8> background)>`, which **L4 `app`** binds to `render::render_document_srgb8` at bootstrap. `commands → render` is not merely undeclared but **level-inverting** (`render` is L2), and §9 puts "the bulk" of logic in L1 — injecting the one impure step keeps the plan/refusal/cancel/progress matrix in headless Catch2 reach with a stub renderer. This is A18's opaque-`std::function` inversion, applied one level down. **(2) The image value type.** `Srgb8Image` (`src/render/ace/render/render.hpp:19-23`) names no libarbc/GL type, so it moves to **L0 `base`** (`ace/base/image.hpp`, §8's "value types") with `ace::render::Srgb8Image` kept as an alias — one definition shared by the component that fills it and the component that encodes it, instead of two that can silently disagree on stride/alpha/channel order. `base` gains no dependency and stays libarbc-free. **(3) The encoder — the editor's *encode line*.** Nothing in the tree can be reused: libarbc is decode-only and states encoding is the host's job (`arbc/runtime/offline_sequence.hpp:68`), and the only PNG writer in the build ships inside `imgui_test_engine`, reachable only from ImGui-linked targets (L3/L4 under A8) with static symbols. So the editor vendors **one** single-header, public-domain `stb_image_write.h` at `third_party/stb/`, compiled into **exactly one TU** (`src/commands/png_encode.cpp`) with `STB_IMAGE_WRITE_IMPLEMENTATION` + **`STBI_WRITE_NO_STDIO`**, emitting bytes via `stbi_write_png_to_func` with the compression level and filter mode pinned explicitly rather than inherited from stb's mutable globals. Linked with the header-only-**PRIVATE** idiom the build already documents for nlohmann on `ace_project` (`CMakeLists.txt:182-187`), and **contained by CI**: `scripts/check_levels.py` gains `EXTERNAL_RE["stb_write"]` + `EXTERNAL_ALLOWED["stb_write"] = {"commands"}`, the editor's mirror of libarbc's codec line (`plugins/imdec/CMakeLists.txt:11-17`, same pre-cleared licence class). `STBI_WRITE_NO_STDIO` is what makes A3 structural rather than aspirational — with no `FILE*` compiled in, the encoder **cannot** bypass `platform::FileSystem`, so the WASM port has one seam and no leaks around it. Vendoring rather than fetching also pins the bytes, which is what makes a byte-exact `.png` golden a real assertion. **(4) The job.** One thread from `platform::Threads::spawn` — the faculty whose own charter names this consumer (`src/platform/ace/platform/threads.hpp:19-21`, *"the later async export-with-progress"*) — **not** the `WriterThread` (A4.1b posts *writes*; queuing a multi-second render behind the writer's FIFO stalls every edit, the coupling A18 rejects) and **not** libarbc's shared `WorkerPool` (A5 gives it to the interactive renderers). Progress crosses to the UI thread as an A18 immutable published value (`std::atomic<std::shared_ptr<const ExportProgress>>`), cancellation as a separate atomic checked **between** items (`render_offline` exposes no cancellation hook, and pretending otherwise would mean lying in the UI). The job is a pure reader — no `transact`, no journal entry, not undoable (D15) — and is **joined inside the shell scope that encloses the `Document`'s lifetime** (`src/app/shell.cpp:210-213,245-246`). Because `render_offline` pins the current version per call, batch coherence is **reported, not enforced**: the report carries the start/end `revision()` and a `document_changed_during_export` flag. The **filled background** composites in the **linear working space inside L2 `render`** (using `arbc/media/pixel_traits.hpp`'s library-owned transfer), never as an sRGB8 blend — D10 makes the editor the invisible translator, and a gamma-space `over` is wrong in a way a golden written from the same wrong code cannot catch. Transparent stays the default, on which path the export renderer is byte-identical to the shipped `render_document_srgb8`. **UI:** the already-catalogued `dockmodel::ViewType::Export` (`view_registry.cpp:24`, half of D21's **Review** preset) gets its real body through `views::register_view_body` — a **panel, not a rail modal**: export carries persistent state (a camera tick-list, options, a live progress readout), which is the inverse of A16's "inserting is a one-shot confirmed op" reasoning, and `views` already reaches `commands`/`render`/`scene`, so no `ProjectGateway` marshalling applies. The destination defaults to `ProjectLayout::exports_dir` and is overridden through the **shipped** `app::FolderDialog` folder seam (A12) injected as a callable — D14 names files by camera, so the user picks a directory, never a filename, and no `SDL_ShowSaveFileDialog` surface is introduced. **No new component, no new DAG edge, no libarbc fork, no pin bump** — one vendored header confined to one component, one type moved down to `base`, and one new entry point in each of `commands`/`render`/`views`. *Alternative rejected:* put the whole pipeline in L2 `render` — legal and injection-free, but it moves plan/filename/batch/report/job policy into the component chartered "HostViewport/InteractiveRenderer glue · frame-sync · tile→GL" and out of §9's L1 bulk. *Alternative rejected:* a new L1 `exporter` component — `writer` earned its component by being structurally different (libarbc-free by construction); an export kernel has exactly `commands`' existing dependency set. *Alternative rejected:* hand-roll a PNG writer over stored deflate blocks — no dependency, but ≈raw-size files (a 4K hero at ~33 MB) when the deliverable *is* the file; or a full deflate — hundreds of lines of compression code the editor would own forever. Realized by `editor.cameras.export`; the contact sheet (D14's tiled variant, which needs an embedded glyph table for its captions and a working-space tile resample) is the additive follow-up `editor.cameras.contact_sheet`. *(Amended by `editor.cameras.contact_sheet`: the projected working-space tile resample is **not** what shipped — a tile is rendered AT tile resolution through this row's own injected `RenderFn`, so there is no resample, no `arbc/media/image_resampler.hpp` consumer and no new L2 entry point; see A21. The embedded glyph table stands.)* *(Amended by `editor.cameras.export_pinned`: as of the v0.4.0 pin, batch coherence is **enforced, not reported**. `run_export` takes ONE `arbc::DocStatePtr` for the whole batch (`Document::pin()`, `arbc/runtime/document.hpp:375`) and renders every item **and** the contact sheet against that single frozen version through libarbc #27's caller-pinned overload `render_offline(document, pinned, viewport, backend)` (`arbc/runtime/offline.hpp:42-45`), so an edit landing mid-batch can no longer make item 3 reflect a state item 1 did not. The pin rides through the injected `RenderFn` as a leading argument, preserving this row's L1→L2 inversion — `commands` holds an `arbc::DocStatePtr` value and hands it to the opaque render callable, still never calling `render`. `ExportReport::document_changed_during_export` is **retained but repurposed**: with one pin the batch IS coherent, so the flag no longer warns about the output — it is an informational note that the live document advanced past the exported (pinned) version, and its `src/views/views.cpp` notice is reworded to say what is now true. No new component, no new DAG edge, no new dependency — the same kernel threads one more value through the same injection.)* *(Amended by `editor.cameras.truetype_captions`: the ledger now carries a SECOND vendored single-header — `third_party/stb/stb_truetype.h`, public-domain, instantiated in exactly ONE `commands` TU with `STB_TRUETYPE_IMPLEMENTATION` under the identical PRIVATE-include idiom as `stb_image_write.h` and pinned by a new `EXTERNAL_ALLOWED["stb_truetype"] = {"commands"}` (+ its `EXTERNAL_RE` twin) — so "one vendored **encode** dependency" still holds literally (`stb_image_write` remains the sole encoder) while the vendored-header count is now two. And the embedded glyph table this row's `editor.cameras.contact_sheet` amendment said "**stands**" is RETIRED: offline captions rasterize a build-embedded Inter face instead. See A27.)* |
| A21 | **The contact sheet composes in L1 `commands` by COPYING pixels, not filtering them: each tile is rendered at tile resolution through A20's injected `RenderFn`, and its captions come from an embedded ASCII bitmap glyph table — no resampler, no libarbc media include, no new L2 entry point, no new external dependency.** D14 (`docs/00-design.md:481`, §9 prose `:359-361`) promises *"a **contact sheet** (all cameras tiled into one image) falls out for free"*, and A20 deferred it to `editor.cameras.contact_sheet` projecting a *working-space tile resample*. That projection is superseded, for three reasons. **(1) A tile is a render, not a downscale.** D14 defines the N× multiplier as *"render this camera above its set resolution"* and A20's kernel already parameterizes the render by output size (`std::function<base::Srgb8Image(const arbc::Affine&, int w, int h, std::optional<Rgba8>)>` plus the injected `interact::viewport_camera_for_shot`); asking for a 240×135 frame of a 3840×2160 camera is the identical operation in the other direction. It also yields a *better* image — the compositor samples each source with libarbc's own working-space resampler at the requested scale, where render-then-decimate samples at full resolution and then applies a second, editor-authored filter on top. And it is what makes a sheet-only run cheap: twelve thumbnails instead of twelve 8-megapixel frames with their `w*h*20`-byte working buffers, so `k_max_export_bytes` can never refuse a tile. The rejected path was not merely more code but *structurally* more: `arbc/media/image_resampler.hpp` is a per-pixel tap kernel (`decimate_half_band` is a 2:1 half-band decimator, `sample_bicubic` a magnifier), so an arbitrary-ratio downscale means an editor-owned mip chain with its own quality decisions and goldens, and because the linear working surface never escapes `render.cpp` it would need a **new L2 entry point** fusing render+resample+convert. **(2) Composition therefore blends nothing.** The sheet is allocated filled with the chosen background (`{0,0,0,0}` when transparent), tile rects never overlap, and every tile is rendered with the *same* background option the sheet is filled with — so under each tile the destination is either fully transparent or exactly that tile's own opaque backing, `over` degenerates to `replace`, and the correct composite is reached with **zero colour arithmetic in `commands`**. This is the strongest available answer to the D10 hazard A20's filled-background clause names: not "blend correctly in L1", not "move the blend to L2", but eliminate it — and it is testable as a byte-identity between each tile rect and an independent render of the same camera, which any later filter, premultiply or blend breaks immediately. **(3) Captions are a `constexpr` table, not a dependency.** §8 makes `views`/`dock` the only layer that sees ImGui, so a headless L1 job has no font atlas to borrow; `commands` embeds 95 ASCII cells of 5×7 glyphs in a 6×8 box (760 bytes, `src/commands/glyphs.cpp`), integer-scaled by pixel replication, each set bit written as opaque white over an opaque black shadow at `(+scale,+scale)` — legible on light, dark, mid-grey and transparent backgrounds **without analysing the background**, so no sRGB→linear transfer enters `commands`, and 1-bit glyphs introduce no alpha ramp that would reintroduce a blend. Unmapped bytes (including every byte of a multi-byte UTF-8 sequence) collapse to one fallback box per run. A20's ledger — the editor vendors exactly **one** encode dependency — stays true. *(Amended by `editor.cameras.caption_latin1`: the table covers **U+0020–U+007E and U+00A0–U+00FF** — 191 cells, 1528 bytes, stored as two contiguous blocks so the original ASCII bytes are untouched — and the caption face iterates **decoded code points**, not bytes: `commands` decodes UTF-8 in-place, an ill-formed maximal subpart decoding to U+FFFD. The fallback box and its **one-per-run** rule stand, now over unmapped **code points** (so `"中文"` is still one box and `"Café"` is now four glyphs). Still no rasterizer, no `assets/` font, no second external — A20's ledger is unchanged.)* **Shape:** the sheet is a second *phase* of A20's existing `ExportService` job, sharing its thread, its A18-published progress, its between-unit cancel and its report; `ExportOptions` gains `write_items` (default true), `contact_sheet` (default false) and `tile_edge` (default 256, clamped `[32,1024]`), so the shipped behaviour is the default path unchanged. Layout is a pure function of `(N, tile_edge)`: `ceil(sqrt(N))` columns, a square tile box per camera with its render aspect-fitted and centred, a fixed 8px gutter, a caption strip per cell; the whole sheet is bounded by A20's `k_max_export_bytes` and **refused as a value** when over, while the batch phase still runs. Cancellation abandons the sheet before encode — a half-composed grid is byte-valid PNG and indistinguishable from a complete one, the one failure a user cannot detect. **No new component, no new DAG edge, no new external dependency, no libarbc fork, no pin bump, no new thread** — `scripts/check_levels.py` is unmodified. *Alternative rejected:* the projected render-at-native-then-resample-in-L2 path — defensible, but it buys a worse image for a new L2 entry point, an editor-owned filter and a second golden, and its stated motivation (*"not an sRGB8 box filter"*) is satisfied more completely by having no filter at all. *Alternative rejected:* a box filter over the `Srgb8Image` in `commands` — the gamma-space error A20's filled-background clause exists to prevent. *Alternative rejected:* vendoring `stb_easy_font` or a TTF rasterizer for captions — a second external, and both emit geometry or antialiased coverage whose grey edge pixels are exactly the blend this row removed, for text measured in single-digit pixels. Realized by `editor.cameras.contact_sheet`. *(Amended by `editor.cameras.truetype_captions`: clause (3) is SUPERSEDED — the 191-cell bitmap table (`src/commands/glyphs.cpp`) and its one-per-run fallback box are retired and offline captions rasterize a build-embedded **Inter Regular** face (static `glyf`, OFL 1.1) through vendored `stb_truetype.h`, so an unmapped code point renders the FONT's own `.notdef` and the printable set is the face's native coverage, not a hand-drawn range. Clause (2)'s copy-only rule is NARROWED to the tile IMAGE composite — the per-tile `std::copy` byte-identity law stands there — while caption TEXT becomes an 8-bit-coverage composite over the strip background in the **linear** working space (libarbc's `srgb8_to_linear`/`linear_to_srgb8`, the transfer `render` already uses), keeping the white-glyph + black-shadow legibility pair. This POPULATES the `assets/ … fonts (bundled)` slot (§7, `:300`) this row's `caption_latin1` amendment said stayed empty and adds a second vendored header — see A27 for the bundled-font architecture and A20 for the ledger.)* |
| A22 | **A process runs in exactly one of two modes, chosen once at bootstrap: *project mode* (A7 verbatim — one `Document` for the process lifetime) or *launcher mode* (ZERO `Document`s for the process lifetime). No process ever transitions between them.** D26's welcome needs a window before any project exists, which A7's "the app owns exactly one `Document` for its lifetime" does not describe — so A7 is **refined, not broken**: it continues to govern every process that holds a project, and the pre-project launcher is the one process shape that holds none. A launcher spawns a sibling (`commands::open_another_project`, D19) and **exits**; it never mints a `Document`, so there is no second-`Document` window and no in-process swap for the sibling to be confused with. **The branch is a pure predicate evaluated before any work.** `ace::app::wants_welcome_launcher(const ShellOptions&)` is `project_dir.empty() && !headless` — the two facts D26 names — and `run_editor` (`src/app/shell.cpp:192`) dispatches on it *before* it constructs a `NativeFileSystem`, resolves `scratch_project_dir()` (`:70-74`), or starts the `WriterThread` (`:213-217`). Launcher mode runs `run_welcome_launcher(opts)`, which reuses `Shell::init` unchanged — the whole SDL/GL/ImGui bring-up (`:80-130`) is already `Document`-free, as the `open_ui`/`save_as` e2e rigs prove by standing a `Dockspace` up with no `AppState` — and then constructs **no** `AppState`, `WriterThread`, `CanvasView`, `Dockspace`, `ExportService` or view bodies, and runs no on-close GC. "Zero `Document`s" is therefore **structural** — the code to make one is not on the path — rather than a runtime assertion. Because `headless` gates it, the driven path (`opts.headless = true`, every e2e and the offscreen smoke) keeps `D-app_state-6`'s scratch bootstrap verbatim, and no shipped test changes behaviour. **The welcome chrome is a `dock` type** (`dock::WelcomeWindow`) — a *second host* for the same `ProjectGateway` the rail hosts, so A12's L3-declares / L4-implements / POD-crosses inversion is reused rather than duplicated, and `dock` gains no include beyond its own header + std. New's compose modal is extracted to a dock-local `NewProjectModal` value + one draw function both hosts call (`Dockspace` keeps its existing accessors as forwarders), so `editor.project.dir_is_project` tightens **one** implementation of "parent + typed name → `new_project`". **The gateway splits along the same seam the modes do.** Exactly five of `ProjectGateway`'s virtuals — `open_project`, `new_project`, `open_recent`, `pick_folder`, `recent_projects` — never touch `AppState` (`src/app/project_gateway.cpp:46-84`); every other one does. So L4's `AppProjectGateway` is factored into a session-free base `app::ProjectEntryGateway` (holding the recent store, `FileSystem`, `FolderDialog`, `ProcessLauncher` and executable path; implementing the five entry verbs, the private `spawn`, and inert answers for the session virtuals) with `AppProjectGateway final : ProjectEntryGateway` adding the `commands::AppState&` and the session overrides. Launcher mode constructs the base. Making the session requirement a **type** is what keeps the invariant absolute: a launcher's gateway cannot reach a `Document` because it does not hold one. **No new component, no new DAG edge, no new external dependency, no libarbc fork, no pin bump** — `scripts/check_levels.py` is unmodified; one new header/TU pair in `dock`, one base class in an existing `app` header, one predicate and one mode function in `app`. *Alternative rejected:* keep `D-app_state-6`'s scratch default for interactive launches and put the welcome *inside* that scratch project's window — no new mode, but it mints a project directory and a live mmap arena per curiosity launch, and every verb then spawns a sibling and orphans the scratch process, which is the litter D26 exists to stop. *Alternative rejected:* let the launcher open the chosen project **in-process** and become the project window — one process instead of two, but it is a direct D19/A7 violation and would make `Document` lifetime lazy in every consumer that today may assume it. *Alternative rejected:* a standalone `LauncherProjectGateway` beside `AppProjectGateway` — no base class, but two copies of the validate → MRU-front → `spawn` sequence, the one piece of this seam a divergence would silently corrupt. *Alternative rejected:* make `AppProjectGateway`'s `AppState` a nullable pointer with a guard on each session verb — no new type, but ~17 null branches that are unreachable in project mode and unreached in launcher mode, i.e. dead code the coverage gate would have to be argued around. Realized by `editor.project.welcome`. |
| A23 | **The editor owns exactly ONE `arbc::RasterTileStore` per `Document` — minted *with* the `Document` inside `project`, carried on `OpenedProject` into `commands::AppState`, and released *before* it — while the parallel tile dispatches stay NULL because the editor has no `WorkerPool` at save/load time.** libarbc states the rule and leaves the placement to the host: *"The host owns one `RasterTileStore` per `Document`"* (`arbc/runtime/builtin_codecs.hpp:154`), and the memo is bound into the raster codec **by closure** (`builtin_codecs(registry, tiles)`, `arbc/runtime/document_serialize.hpp:134`) and **seeded by the load** (`load_document(..., tiles, decode)`, `:217-220`). Three placements were possible and only one keeps the "one per `Document`" invariant structural. **(1) Ownership is born with the `Document`.** `project::open_project` / `create_project` mint the store and return it on the existing `OpenedProject` POD, so no code path can produce a `Document` without its memo, and `commands::AppState` — already the process's single project session (A7), already the owner of the persistent `Registry` and document-scoped `KindBridge` — receives it through the move it already performs (`src/commands/app_state.cpp:159-169`). This **reverses `D-save-2`'s projection** that `editor.paint.*` would own the store: painting is not the only producer of tiles (any raster-cell insert, paste or import mints them), and a memo owned by a *tool* would be absent from every headless save. **`RasterTileStore` holds owning `BlockRef` pins into the `Document`'s `BigBlockPool`** (`arbc/runtime/raster_tile_store.hpp:141`, *"THE validity token: while this lives, the slot cannot be recycled"*), so it is declared **after** `document_` in every owner (reverse-destruction order) and any move-assignment must release it first — a lifetime rule, not a style preference. **(2) The store is a `project` parameter, never an upward reach — and it is scoped to ONE asset destination.** `save_project` takes a trailing `arbc::RasterTileStore* tiles = nullptr`; §8 lets `project` (L1) name `arbc::` types, and a null pointer is the library's own documented, correct-but-non-memoizing degradation (`document_serialize.hpp:134`), which keeps every existing call site and test source-compatible. `save_project_as` deliberately takes **no** store: the raster codec resolves a memo HIT entirely inside the memo, dispatching no encode and therefore never handing the tile to the `AssetSink` at all (`arbc/runtime/codec_raster.cpp:155`; only misses reach the reap that calls `store_asset`), so "I hashed this tile last pass" is usable as "…so its blob is already on disk" ONLY when this pass writes to the same `assets/`. Plain Save always does — `AppState`'s `layout_` never changes (D-save_as-2) and the memo is either born cold or seeded from that very root's canonical — but a Save As publishes into a FRESH root, where a shared memo would emit a `project.arbc` naming tile blobs nobody ever wrote there: a silently pixel-less copy, the same data-loss class D-save_as-4's clobber guard exists to prevent. A copy is worth one full re-hash (user-initiated, once per invocation), and the session's own memo is left intact so the next plain Save stays incremental. **(3) `SaveContext::set_storage_format(doc.storage_format())` is part of the seam, not an aside.** The memo keys on the on-disk storage format (`RasterTileStore::begin_pass(ctx.storage_format())`, `arbc/runtime/codec_raster.cpp:117`; `seed(..., storage, hash)`, `:430-438`) and a successful load installs the file's authored format onto the `Document` (`document_serialize.cpp:755-759`). Saving through a `SaveContext` left at its own default therefore both misses every seeded entry **and** silently re-authors a 32f painting at 16f, renaming every blob — so honoring `doc.storage_format()` (arbc's own idiom, `document_serialize.cpp:431`) is the precondition that makes memoisation observable rather than an optimisation on top of it. **(4) The dispatches stay null — this is A4, not an omission.** A worker-backed `TileDecodeDispatch`/`TileEncodeDispatch` requires an `arbc::WorkerPool`, and the editor's only pool belongs to `ace::render::CanvasHost` (`src/render/ace/render/canvas_host.hpp:64-70`) — **L2**, which L1 `project`/`commands` may not depend on (§8), and which does not yet exist when `open_or_create_app_state` runs. Minting a second pool for save/load would contradict A4's *shared pool* clause and add an editor-owned thread `D-open-7`/`open.md` Constraint 7 explicitly forbids. A null dispatch decodes/encodes **inline, byte-identically** to the serial path (`document_serialize.hpp:212`), so the null is a *complete* implementation of the seam at the editor's current concurrency shape, and the memo — which is where the O(all tiles) cost actually lives — is fully realized without it. **No new component, no new DAG edge, no new external dependency, no libarbc fork, no pin bump**: `arbc/runtime/raster_tile_store.hpp` is an exported public header of the pinned v0.4.0 and pulls no JSON, so `ace_project`'s link line is unchanged and `scripts/check_levels.py` is unmodified. *Alternative rejected:* a store owned by `commands::AppState` alone, with `project::open_project` gaining an in-parameter — same end state, but the store's existence becomes a caller obligation, so a second `open_project` caller (a future import/probe path) can legally produce a memo-less `Document`. *Alternative rejected:* a store owned per-save (constructed inside `save_project`) — trivially correct and needs no plumbing, but a memo that dies with the pass memoizes nothing; incrementality is precisely the cross-save property. *Alternative rejected:* give `project` its own small `WorkerPool` for a parallel decode at open — measurably faster cold opens on large raster projects, but it is a second pool inside a process whose concurrency A4 says is the library's verbatim, on the one path (rebuild-from-canonical) that runs at most once per process. Realized by `editor.project.raster_tile_store`. |
| A24 | **Project-entry outcomes are VALUES, not inferences: the three entry verbs of `dock::ProjectGateway` return a dock-local `ProjectEntryOutcome { succeeded, refused_target, spawn_failed }` (refines A12's "errors are values" clause).** A12 inverts project entry behind an L3-declared, L4-implemented gateway and states *"Errors are values: the mutating actions return success/failure the rail renders as inline feedback"* — but a single `bool` conflates the two failures D22's spawn model makes structurally distinct: the gateway **refused the target before launching anything** (not a project · no longer a project · an invalid name · a target that already exists) and the target was accepted but the **sibling `exec` failed**. Their corrective acts are opposites — pick a different folder, versus nothing in the folder picker will help — and A12's own dependency inversion is what makes the difference unrecoverable at the call site: `dock` may include neither `<ace/project/…>` nor `<ace/platform/…>`, so a refused L3 host **cannot ask a second question**. The answer must arrive with the refusal. **What shipped before this row is the evidence.** `dock::WelcomeWindow` reconstructed the discriminant from MRU membership (a directory still listed after a refusal must have validated, so the spawn is what failed) — sound about the code it cited and unsound as a discriminator, because `RecentProjects::add`'s failure is discarded (`src/app/project_gateway.cpp:55,87`) and `recent_projects()` re-prunes through `project::is_project_directory` on every query (`:96-102`), each of which flips it in a reachable case; while `dock::Dockspace`'s tool rail did not discriminate at all and reported a failed `exec` as *"That folder is not a project."* — two hosts of one seam producing different, and on the rail actively wrong, messages for the same failure. **Shape.** A scoped enum in `src/dock/ace/dock/dock.hpp`, in the `GcSummary`/`InsertKindSpec`/`ComposeModalSpec` dock-local-exchange family: it names no `project`/`platform`/`commands`/`arbc` type and adds no include, so `dock`'s include set is unchanged. The verbs stay **pure** virtuals — every override carries `override`, so the changed return type is a hard compile error at the L4 impl and at all eleven test fakes, which is the forcing function; a defaulted virtual would let a stale `bool` override silently stop overriding. `ProjectEntryGateway::spawn` returns the outcome directly, so `spawn_failed` is produced in the one place holding `commands::open_another_project`'s `error_code`. Both hosts route through one L3 pure mapper, `entry_feedback(outcome, refused_message)` — `""` / the caller's per-verb refusal string / the single `"Could not start the editor."` literal — which is what makes host divergence impossible rather than merely fixed, and which is unit-testable with no ImGui context. **Refusals are deliberately NOT split by cause**: `D-dir_is_project-6` settled that an invalid name and a taken name share one string because they call for the identical corrective act, and the same reasoning generalizes to every pre-launch refusal — the enum splits by corrective act, not by cause. The four user-visible strings are unchanged D22/D26 copy; this row changes *when* each is shown, never *what* it says. **The session verbs (A13) are out of scope**: `save_as` — which shares the compose modal with `new_project` (D27 / D-dir_is_project-5) — is adapted at its one L3 binding (`false → refused_target`, observably identical today, since all four of its failure modes already produce one string), with the honest widening deferred to `editor.project.save_as_outcome`, which needs a fourth enumerator and a richer result from `commands::save_project_as`. **No new component, no new DAG edge, no new external dependency, no libarbc fork, no pin bump** — `scripts/check_levels.py` is unmodified; one enum, one free function and one field on types `dock` already owns. *Alternative rejected:* `std::expected<void, std::error_code>` — the house idiom below L3 and it would carry the OS error for free, but mapping an open-ended `error_code` space onto two strings requires knowing what `commands::open_another_project` can return, i.e. the include A12 forbids. *Alternative rejected:* keep `bool` and add a `last_entry_outcome()` query — no fake churn, but it turns a value-returning seam into a stateful two-call protocol nothing forces a fake to keep consistent. *Alternative rejected:* repair only `WelcomeWindow` (the literal reading of the source-of-debt note) — leaves the rail, the surface more users see, telling a user with a broken install that their project folder is not a project. Realized by `editor.project.entry_outcome`. |
| A25 | **The outcome seam covers the one SESSION verb that also creates a project: `save_as` returns `ProjectEntryOutcome` too, the enum gains a fourth enumerator `publish_failed`, and `commands::save_project_as` returns the STAGE it stopped at (refines A24, which scoped session verbs out).** A24 widened the three entry verbs and left `save_as` — a session verb (A13) sharing New's compose modal (D27 / `D-dir_is_project-5`) — adapted at its one L3 binding with `false → refused_target`, naming `editor.project.save_as_outcome` as the payer. That adapter was observably a no-op *and* observably wrong: `save_as`'s own doc comment lists four ways it returns false (an invalid name · a target that already exists · a failed publish · a failed `exec`), and the rail answered all four with *"Enter a project name that does not already exist here."* Two of those are not name problems. A failed publish (`SaveError::SerializeFailed`/`AssetWriteFailed`/`IoError`, `src/project/ace/project/save.hpp:33-37`) is a full disk or an unwritable parent, which retyping cannot fix; and a failed `exec` is worse than merely mislabelled, because `commands::save_project_as` publishes *before* it execs (`src/commands/app_state.cpp:223-233`), so a spawn failure leaves a **complete, valid copy on disk** — the user is told to type a different name, and the obvious retry with the same name then hits D27's existing-target guard (`src/project/save.cpp:205`) and produces that same message for an entirely different reason. **The discriminant is genuinely absent, not merely unread.** `platform::Result<T>` (`src/platform/ace/platform/result.hpp:14-17`) holds EITHER a value OR a `std::error_code`, so a failed call structurally cannot also report that the copy reached disk; and the codes overlap across stages — the canonicalize fault (`app_state.cpp:211-213`) and the `spawn_detached` fault (`:231-233`) are both plain system/generic codes, so error-sniffing would replace one wrong message with another. So `commands::save_project_as` returns `SaveAsResult { SaveAsStage stage; std::error_code error; project::SaveOutcome published; }` with `stage ∈ { refused, publish_failed, spawn_failed, spawned }` — the one function whose return type changes; `project::save_project_as` and `commands::save_project` keep `platform::Result<SaveOutcome>`. **The refused-versus-fault split happens in L1 `commands`**, the level that owns both vocabularies (`std::errc::file_exists` from the D27 guard, `SaveError` from `project` — the pairing `D-dir_is_project-2` already blessed on one channel); **L4 `app` only translates**, mapping the L1 stage onto the dock enum with a total `switch`, which is verbatim the `GcOutcome`→`GcSummary` re-vocabularization at `src/app/project_gateway.cpp:191-205` and the only thing that level can do that no other can. `dock` therefore still includes neither `<ace/project/…>` nor `<ace/commands/…>`, exactly as A12 requires. **Strings.** `entry_feedback` gains a `publish_failed` case owning one literal, `"Could not save a copy there."`, and a **defaulted** third parameter `spawn_failed_message = "Could not start the editor."`. That default is what keeps A24's one-mapper guarantee intact rather than reopening it: A24 made divergence between two HOSTS of one verb impossible, not every verb identical, and it already grants that the refusal wording is per-verb context (*"Open says one thing, Recent another"*). After a Save As the copy is on disk, so exactly one call site opts in with *"Saved the copy, but could not start the editor."*; every other call site takes the default and every entry-verb string is byte-identical. The refusal string is untouched — D27's *"same single refusal message"* rule is about refusals, and this row adds no refusal. **Purity is again the forcing function** (A24 / `D-entry_outcome-3`): `save_as` stays a pure virtual, so the return-type change is a hard compile error at the inert `ProjectEntryGateway` override, at `AppProjectGateway`, and at every test fake. The compose modal's close policy is unchanged — `succeeded` closes, everything else leaves the modal open with the parent the async pick resolved. **No new component, no new DAG edge, no new external dependency, no libarbc fork, no pin bump** — `scripts/check_levels.py` is unmodified; one enumerator, one L1 POD, one defaulted parameter and one field on types the levels already own. *Alternative rejected:* a separate `SessionVerbOutcome` enum — symmetric naming, but the shared compose modal binds one `std::function` that both New and Save As satisfy, so it would have to be templated or duplicated over two vocabularies agreeing on three of four cases. *Alternative rejected:* keep `platform::Result` and add a `commands` error code for the spawn stage so L4 discriminates by value — smaller and on the house `SaveError` idiom, but the mapping degenerates to an `else` fallthrough that silently swallows any error added later, and "spawned" remains expressible only as the absence of an error. *Alternative rejected:* a `SaveAsStage*` out-parameter — zero churn at the L1 call sites, but it contradicts A12's "errors are values" and makes the stage optional to read, which is the exact failure mode being paid off. *Alternative rejected:* leave `spawn_failed` on the shared literal — one fewer field, but it ships the retry loop above with a message that is incomplete rather than wrong. Realized by `editor.project.save_as_outcome`. |
| A26 | **The `platform::FileSystem` seam gains its first destructive primitive — `remove_tree` — and the ONE call site is `project::save_project_as`'s post-guard failure path, so a failed Save As leaves no directory behind (completes D27's create-verb contract; narrows the "the editor never deletes through the seam" posture of `D-gc-2`).** D27 says a creation verb takes a not-yet-existing target and refuses one that exists; `project::save_project_as` enforces that with `fs.exists(target_root)` (`src/project/save.cpp:205`) and then publishes into the target, and the publish is what *materializes* the directory — `save_project`'s `fs.make_directories(layout.assets_dir)` (`:106-108`) creates `target_root` on the way to `assets/`. So a `SaveError` from the publish (`:230`) or from the trailing `.gitignore` write (`:237`) returns a failure while leaving a directory on disk, and D27's own guard then refuses the identical retry **forever**: the user is told *"Could not save a copy there."* (A25), retypes nothing, tries again, and gets *"Enter a project name that does not already exist here."* — the second message a lie caused by the first failure. A25 made the *message* honest and named this row's leaf as the payer of the *state*. **The primitive, not a raw call.** `virtual std::error_code remove_tree(const std::filesystem::path&) const` joins `exists`/`list_directory`/`read_file`/`write_file`/`make_directories`/`atomic_replace` (`src/platform/ace/platform/filesystem.hpp:18-46`), because the alternative — `std::filesystem::remove_all` inlined in `project` — is invisible to the seam in exactly the two ways that matter: it cannot be fault-injected (the six `FileSystem` doubles are how every disk failure in this codebase is provoked, e.g. `tests/save_as_test.cpp:132`), and it hard-codes a POSIX unlink into the one component the A3 WASM port swaps wholesale for the File System Access API / OPFS, whose `removeEntry({recursive: true})` is the natural implementation. It is **idempotent** (an absent path is success, mirroring `make_directories`'s "succeeds if the directory already exists") and rejects an empty path with `std::errc::invalid_argument`, mirroring `commands::save_project_as`'s own empty-target guard (`src/commands/app_state.cpp:200-203`). This is not a reversal of `D-gc-2`/`gc.md`'s Constraint 8 — asset reclamation still belongs to libarbc's `gc_project_directory`, whose only deletion surface is `remove_tile(hash)` under `assets/tiles/` (`arbc/runtime/asset_gc.hpp`, `arbc/serialize/asset_reaper.hpp`) and which therefore structurally cannot remove a project root — but it does retire that refinement's blanket "**no new `FileSystem` primitive**", which was a scope statement for a leaf that needed none. **Ownership is what makes the delete safe, and it is proved by the guard, not asserted.** D27's `fs.exists(target_root)` returned false immediately before, so every byte under `target_root` was written by this call; removing the root can therefore destroy nothing but this call's own debris. The rollback runs on **exactly the two post-guard failure branches** and on no other path: the D27 refusal removes nothing (the target is somebody else's), plain `project::save_project` removes nothing (it publishes into the session's own live project root, which it did not create), and a `SaveAsStage::spawn_failed` removes nothing — A25 ships *"Saved the copy, but could not start the editor."* precisely because the copy is complete and valid, so rolling it back would destroy good work to tidy up a launcher fault. That boundary is why the rollback lives in **L1 `project`**, below the stage vocabulary, rather than in `commands` where the stages are named. **A failed rollback reports the ORIGINAL error**, never a new one: the actionable fact is that the copy failed, the debris is the pre-existing behaviour, and a `SaveError::RollbackFailed` would change `SaveError`'s meaning from *why the save failed* to *what state the disk is in* and hand the user a message about a directory they never asked for. **No new component, no new DAG edge, no new external dependency, no libarbc fork, no pin bump, and no change to any outcome vocabulary** — `scripts/check_levels.py` is unmodified (`project → platform` is an existing edge, `ALLOWED["project"] = {base, platform}`), `ProjectEntryOutcome`/`SaveAsStage` are untouched, and no L3/L4 code changes; the user-visible change is that the retry the messages already invite now works. *Alternative rejected:* raw `std::filesystem::remove_all` in `save.cpp`, on the `D-gc-2` precedent — but that precedent is "*libarbc* owns deletion", and here nothing below owns it; taking it would put an untestable, un-WASM-able unlink in L1. *Alternative rejected:* a narrower `remove_project_tree(layout)` that only unlinks the known D16 members — safer-looking, but it silently leaves anything else the publish wrote (or a future layout member) behind, which is the same half-state under a new name. *Alternative rejected:* rolling back in `commands::save_project_as` where the stage is known — it would put the cleanup one level above the guard that proves the cleanup is safe, and `project::save_project_as` is the primitive every future host (a CLI, the WASM port) calls directly. *Alternative rejected:* leaving the debris and teaching D27's guard to adopt a directory it recognises as its own failed attempt — it makes the create verb adopt, which is the exact thing D27 forbids, and "recognises" has no sound definition. Realized by `editor.project.save_as_rollback`. *(Amended by `editor.project.create_rollback`: this row's title over-claimed — D27 names **two** create verbs, and `save_as_rollback` paid only **Save As**. The **New Project** half — `project::create_project` (`src/project/project_open.cpp:301-352`), which has the identical shape: a D27 existence guard at `:312-314`, then a scaffold with four early returns — is completed here, so the "**ONE call site**" claim is superseded: `remove_tree` now runs on `create_project`'s **four** post-guard failure branches too (the mkdir loop `:320`, the `.gitignore` write `:326`, the workspace-document mint `:334`, the checkpoint `:339`), all still in **L1 `project`**, so `scripts/check_levels.py`, `ALLOWED["project"]` and every outcome vocabulary stay untouched. Every A26 rule carries over verbatim — the guard proves the delete is safe (`fs.exists(root)` was false four lines earlier), and a failed rollback reports the **original `OpenError`**, never a new `RollbackFailed`. **One rule is new, because the create path is not a pure-write path:** the checkpoint branch (`:337-339`) holds a **live, mmap-backed `arbc::Document` over `workspace/`** in a local `std::unique_ptr` with a running `HousekeepingThread`, so its rollback must **destroy that `Document` before `remove_tree`** — remove the mapped `workspace/` subtree out from under a live mapping and the removal is unsafe (Windows cannot unlink a mapped file; POSIX strands the mapping). The three earlier branches hold no live `Document` and roll back with a bare `remove_tree(root)`. Realized by `editor.project.create_rollback`.)* |
| A27 | **Offline captions rasterize a build-embedded TrueType face in L1 `commands`: Inter Regular (static `glyf`, OFL 1.1) is turned into a linked `const` byte array by a CMake build step and rasterized by vendored `third_party/stb/stb_truetype.h`, retiring A21's `constexpr` bitmap glyph table — antialiased 8-bit coverage composited over the caption background in the LINEAR working space, no runtime asset load, no new component, no new DAG edge.** A21 clause (3) chose a hand-drawn `constexpr` table because §8 makes `views`/`dock` the only layer that sees ImGui, so a headless L1 job has no font atlas to borrow; `editor.cameras.caption_latin1` extended it to Latin-1 and the parking-lot triage (2026-07-28, human sign-off) decided the table's growth is now a bundled font, not a wider hand-drawn range. Four facts had no home. **(1) A real face, not a wider table.** A hand-drawn table has a charset cliff at whatever range was last drawn (Latin-1 today, so `Łódź`/`Škoda`/Greek/Cyrillic still box); a face covers its native repertoire and reports its own `.notdef` for the rest, so the missing-glyph box becomes the font's business and the editor stops legislating which of a user's typed names are drawable. **(2) The face is INTER REGULAR under OFL 1.1, static `glyf`.** Inter is designed for small-size legibility and to work UNHINTED — exactly the regime `stb_truetype` imposes (it ignores hinting) — and carries **NO reserved font name**, the load-bearing licence property: OFL 1.1's RFN clause forces a rename on any modification, and subsetting the face IS a modification, so a face with an RFN could not be vendored-and-subset without a rename. Inter ships VARIABLE by default and `stb_truetype` has no variable-axis support, so the vendored file is the STATIC `Inter-Regular.ttf` with TrueType `glyf` outlines (stb's best-tested path) — never `InterVariable.ttf`, never a CFF/OTF build. **(3) EMBED at build, do not load at runtime.** The `constexpr` table's quiet virtue was that it was compile-time data — no file I/O, no runtime asset path, nothing for a headless `commands` job to fail to find, and no `FILE*` route around `platform::FileSystem` (A3). A CMake step turns `assets/fonts/Inter-Regular.ttf` into a generated `const std::uint8_t[]` TU linked into `ace_commands`, so the face is linked in exactly as the table was; a runtime font path would make caption rendering depend on installed-artifact layout — a new failure mode for the offline job and a regression against A21's reasoning. The `.ttf` fills the `assets/ … fonts (bundled)` slot (§7, `:300`) reserved and never filled; nothing reads `assets/` at runtime, it is a build INPUT. **(4) Coverage is a blend, so text — and ONLY text — composites in linear.** `stb_truetype` rasterizes an 8-bit COVERAGE bitmap, so a glyph edge is an alpha blend, not a copy of an opaque texel: A21 clause (2)'s copy-only invariant (`D-sheet-2`) is NARROWED to the tile IMAGE path (per-tile `std::copy` byte-identity stands) and caption text composites coverage over the strip background in the LINEAR working space using libarbc's own transfer functions (`arbc::srgb8_to_linear`/`linear_to_srgb8`, `arbc/media/pixel_traits.hpp` — already `EXTERNAL_ALLOWED["arbc"] ∋ commands`, already how `render` composites), never as an sRGB8 blend (the D10/gamma-space error A20's filled-background clause names). The white-glyph + black-shadow legibility pair is KEPT — antialiasing makes an arbitrary background easier to read against, not the pair unnecessary. **The containment is the easy part**: `stb_truetype.h` sits beside `stb_image_write.h` under the identical idiom — one instantiating TU, a PRIVATE `third_party` include on `ace_commands`, and one `EXTERNAL_ALLOWED["stb_truetype"] = {"commands"}` + `EXTERNAL_RE` entry confining it exactly as `stb_write` is confined (A20). **This establishes the repo's FIRST out-of-band licence record**: `stb_image_write.h` carries its text inline, which cannot work for a binary `.ttf`, so `OFL.txt` ships beside the font and the vendored set (Inter + `stb_truetype`) is recorded for `editor.packaging.package` to carry INTO the installed artifact — a bundled font whose licence text does not travel with it is the actual legal exposure, created at packaging time. **No new component, no new DAG edge, no new pin bump, no libarbc fork, no new thread** — one vendored header confined to one component, one build codegen step, one new `check_levels` external entry. *Alternative rejected:* a more conservative face — **DejaVu Sans** (Bitstream Vera + Arev, equally permissive, ubiquitous on the Linux v1 target) is a one-line change to the face choice, recorded as the fallback, not a redesign; Inter wins on unhinted small-size legibility. *Alternative rejected:* keep extending the hand-drawn table — re-introduces the charset cliff at an arbitrary line and commits by fiat the very "which ranges" answer the parking-lot item held for a human. *Alternative rejected:* a runtime-loaded font from `assets/` — a new headless failure mode and a `FILE*` path around `platform::FileSystem`. *Alternative rejected:* a variable or CFF/OTF Inter build — unsupported by `stb_truetype`. Realized by `editor.cameras.truetype_captions`. |

## Open / next

- **`foundation.build`** — the scaffold: `CMakeLists.txt` (FetchContent libarbc +
  ImGui + SDL3), the `src/` component skeleton (§7), the `check_levels` lint, the
  Catch2 + ImGui Test Engine harness, and `scripts/gate` — then a runnable window
  rendering a trivial `Document` (proving binding + display). This bootstraps the
  DoD every later leaf inherits (§9).
- ~~**ImGui Test Engine license**~~ — resolved: free under the OSI open-source
  carve-out (decision A10).
- **Extensibility** — runtime plugin-kind loading surfaced in "insert cell".
- **The WASM port** — deferred; the seams (A3) are what keep it a port.
